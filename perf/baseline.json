{
  "T0": {
    "p95": 2.77,
    "maxDrawCalls": 23,
    "recorded": "2026-08-15",
    "reason": "Draw calls re-recorded for P3-T16, which made the harness measure two things it had been missing. (1) `populate` assigned owner with `i % 2` against a FOUR-type unit mix, so type and owner were perfectly correlated and every unit mesh was counted for one side only — the exact bias that understated ADR-0016's own batch figures until P3-T02 re-measured them. Decorrelated, which is +4. (2) The harness kept its own extractor and ran it AFTER `bridge.step` had drained `state.events`, so no death ever reached it and the `blast` overlay never fired; it reads the bridge's snapshot now, which is +1 and also removes a second full extraction per tick the game never pays for. Both are the gate becoming more honest, not the frame becoming more expensive. p95 untouched. T0: 17 -> 22.\n\n22 -> 23 (P5-T15): the `impact` overlay, which is PARITY rows 66-68 — the `heavy`, `bonus` and `splashRadius` the engine has hung on every `attackHit` since the Phase 1 MVP and nothing has ever read. Exactly +1, and +1 is the whole cost: one layer for every hit mark on the frame, on the same terms as the tracer and the blast beside it. It fires HERE, on the four-type MVP mix, because that mix is `skiff`/`bastion`/`lancer` — the engine's own counter triangle — so `bonus` hits land wherever two of them meet, without the scene being packed. p95 untouched: this container measured 2.51-3.13 ms across runs on both sides of the change, which is scatter, not signal.\n\nP6-T07 — the camera path now sweeps all EIGHT of the rig's yaw snaps inside one gate run, and the structural maxima now include the warm-up frames. Both change what the gate SEES rather than what the frame costs, so both move draw calls and neither moves p95.\n(a) The path held one yaw for six seconds against a ten-second gate, so every gated measurement this project has ever recorded looked from snaps 0 and 1 and never from the other six. `YAW_HOLD_SECONDS` is 1.25 now: 8 x 1.25 = one revolution per run.\n(b) `runScene` dropped the warm-up frames' draw-call counts along with their timings. A batch count has no warm-up — it is the same on the first frame as on the ten-thousandth, on every machine — so that was not noise removal, it was the first tenth of the camera path being excluded from the only check in this gate that is machine-independent.\np95 unchanged everywhere, and measured rather than assumed: six interleaved gate runs, three of an UNMODIFIED HEAD exported with `git archive` and three of the working tree, alternating so host drift hits both. T0 p95 HEAD 2.76 / 6.16 / 5.16 against MINE 3.02 / 3.93 / 5.10 — the CLEAN checkout blew the 4.27 ms regression ceiling twice and the modified one once, which is the judge's own message playing out exactly as written. T2 HEAD 5.33-5.83 / MINE 5.75-5.78. P2 HEAD 22.04-23.91 / MINE 21.46-22.31. P3 HEAD 4.43-4.66 / MINE 4.67-4.90. P4 HEAD 7.45-8.40 / MINE 7.88-8.28. Every interval overlaps; there is no frame-time signal to record. Three further interleaved pairs once the host quietened settle it: T0 HEAD 2.88 / 3.15 / 3.02 against MINE 2.84 / 3.17 / 2.97, T2 HEAD 5.62 / 5.52 / 5.84 against MINE 5.54 / 5.59 / 5.61 — pairwise indistinguishable, and all three modified runs GREEN on every check. Draw calls were IDENTICAL on all fifteen runs across both hosts and both checkouts, which is what a structural number should be. The lesson for whoever reads a red T0 next: this container reached 6.16 ms on a CLEAN checkout an hour before it reached 2.88 ms on the same one.\n\nT0: 23 -> 23, UNCHANGED, and the zero is worth stating. Nothing was added to this scene — it stays the PRD's own spec with no flags — and neither the yaw sweep nor the warm-up counting moved its peak. Its triangle count did move (13086 -> 12740) because a different rotation puts different entities inside the LOD distance; triangles are not gated and are printed for the record."
  },
  "T2": {
    "p95": 5.16,
    "maxDrawCalls": 25,
    "recorded": "2026-08-15",
    "reason": "Draw calls re-recorded for P3-T16, which made the harness measure two things it had been missing. (1) `populate` assigned owner with `i % 2` against a FOUR-type unit mix, so type and owner were perfectly correlated and every unit mesh was counted for one side only — the exact bias that understated ADR-0016's own batch figures until P3-T02 re-measured them. Decorrelated, which is +4. (2) The harness kept its own extractor and ran it AFTER `bridge.step` had drained `state.events`, so no death ever reached it and the `blast` overlay never fired; it reads the bridge's snapshot now, which is +1 and also removes a second full extraction per tick the game never pays for. Both are the gate becoming more honest, not the frame becoming more expensive. p95 untouched. T2: 19 -> 23.\n\n23 -> 24 (P5-T15): the `impact` overlay, exactly as on T0 above and for the same reason — one layer, not one per mark. p95 untouched (5.80-5.98 ms measured on both sides of the change against a 5.16 ms baseline recorded elsewhere; see the judge's own advice about this container).\n\nP6-T07 — the camera path now sweeps all EIGHT of the rig's yaw snaps inside one gate run, and the structural maxima now include the warm-up frames. Both change what the gate SEES rather than what the frame costs, so both move draw calls and neither moves p95.\n(a) The path held one yaw for six seconds against a ten-second gate, so every gated measurement this project has ever recorded looked from snaps 0 and 1 and never from the other six. `YAW_HOLD_SECONDS` is 1.25 now: 8 x 1.25 = one revolution per run.\n(b) `runScene` dropped the warm-up frames' draw-call counts along with their timings. A batch count has no warm-up — it is the same on the first frame as on the ten-thousandth, on every machine — so that was not noise removal, it was the first tenth of the camera path being excluded from the only check in this gate that is machine-independent.\np95 unchanged everywhere, and measured rather than assumed: six interleaved gate runs, three of an UNMODIFIED HEAD exported with `git archive` and three of the working tree, alternating so host drift hits both. T0 p95 HEAD 2.76 / 6.16 / 5.16 against MINE 3.02 / 3.93 / 5.10 — the CLEAN checkout blew the 4.27 ms regression ceiling twice and the modified one once, which is the judge's own message playing out exactly as written. T2 HEAD 5.33-5.83 / MINE 5.75-5.78. P2 HEAD 22.04-23.91 / MINE 21.46-22.31. P3 HEAD 4.43-4.66 / MINE 4.67-4.90. P4 HEAD 7.45-8.40 / MINE 7.88-8.28. Every interval overlaps; there is no frame-time signal to record. Three further interleaved pairs once the host quietened settle it: T0 HEAD 2.88 / 3.15 / 3.02 against MINE 2.84 / 3.17 / 2.97, T2 HEAD 5.62 / 5.52 / 5.84 against MINE 5.54 / 5.59 / 5.61 — pairwise indistinguishable, and all three modified runs GREEN on every check. Draw calls were IDENTICAL on all fifteen runs across both hosts and both checkouts, which is what a structural number should be. The lesson for whoever reads a red T0 next: this container reached 6.16 ms on a CLEAN checkout an hour before it reached 2.88 ms on the same one.\n\nT2: 24 -> 25 (P6-T07). One layer: `chevron`, Phase 1's veterancy cue. Across all five gated scenes it had been drawing on TEN frames out of three thousand, every one of them here, and every one of them because some unit happened to cross upstream's three-kill threshold mid-run. The yaw sweep landed the peak batch frame on one of those ten, which is the +1.\nThe scene also gains `veterans: 12`, and it costs ZERO — measured both ways on the new path, 25 either way. It is there because the 25 above otherwise rests on a balance accident, and that fails OPEN: the accident going away would drop the true maximum to 24 while the recorded 25 quietly absorbed a real regression. With the knob the layer is drawn deliberately, on all three of upstream's ranks, and the number is a measurement. On the OLD camera path the knob would have been the whole +1 by itself (24 -> 25 with it, 24 without), which is the same number arrived at from either direction."
  },
  "P2": {
    "p95": null,
    "maxDrawCalls": 40,
    "recorded": "2026-08-15",
    "reason": "PRD §5's Phase 2 exit criterion scene — 300 buildings across all 29 types at the T0 tier — and until now the ONLY gated scene with no baseline entry at all. Found while recording P4's (P4-T10). The consequence was not cosmetic: `judge` skips every check it has no baseline for, so P2's draw calls were ungated, and P2 is precisely the scene that validates ADR-0013's six building silhouette families and ADR-0012 §3's hand-written 28-batch cap. A regression that split a family back into per-type meshes — the exact thing those ADRs exist to prevent — would have passed this gate in silence while the scene built to catch it looked green.\n\n35 draw calls, structural and machine-independent, measured identically on every run today.\n\np95 is NULL for P3's and P4's reason, and P2 makes the case better than either: this container measured it at 21.8, 23.9, 24.4 and 24.9 ms early in the session and 34.3 ms later with three subagents competing for CPU — same commit, a 57% spread, straddling the 33 ms budget in both directions. Any p95 written from here would be a coin flip recorded as a fact. The scene stays gated on the PRD's own 33 ms budget, which is machine-independent, and now on its batch count. Record a p95 from a CI run before relying on the regression half.\n\n35 -> 38 (P5-T15), and it is THREE layers rather than one, each of them named because a lump of +3 is exactly the shape a real instancing regression has. (1) `impact`, PARITY rows 66-68, +1 — measured on its own at 36 before the flag below was added. (2) and (3) `selection` and `rally`, +1 each, from the new `selectedProducers: 8` flag: a rally line (row 8) is drawn only for a SELECTED producer the viewer owns, and no perf scene in this repo has ever selected anything, so the layer would have shipped ungated — the fourth time this harness has silently not measured what a task shipped. Selecting also raises the selection ring, which means Phase 1's oldest overlay reaches a gate here for the first time as well. P2 rather than T0/T2 because it is the base-management scene: 300 buildings over all 29 types is the only gated population with enough of the roster's three producers for a realistic multi-building selection.\n\nP6-T07 — the camera path now sweeps all EIGHT of the rig's yaw snaps inside one gate run, and the structural maxima now include the warm-up frames. Both change what the gate SEES rather than what the frame costs, so both move draw calls and neither moves p95.\n(a) The path held one yaw for six seconds against a ten-second gate, so every gated measurement this project has ever recorded looked from snaps 0 and 1 and never from the other six. `YAW_HOLD_SECONDS` is 1.25 now: 8 x 1.25 = one revolution per run.\n(b) `runScene` dropped the warm-up frames' draw-call counts along with their timings. A batch count has no warm-up — it is the same on the first frame as on the ten-thousandth, on every machine — so that was not noise removal, it was the first tenth of the camera path being excluded from the only check in this gate that is machine-independent.\np95 unchanged everywhere, and measured rather than assumed: six interleaved gate runs, three of an UNMODIFIED HEAD exported with `git archive` and three of the working tree, alternating so host drift hits both. T0 p95 HEAD 2.76 / 6.16 / 5.16 against MINE 3.02 / 3.93 / 5.10 — the CLEAN checkout blew the 4.27 ms regression ceiling twice and the modified one once, which is the judge's own message playing out exactly as written. T2 HEAD 5.33-5.83 / MINE 5.75-5.78. P2 HEAD 22.04-23.91 / MINE 21.46-22.31. P3 HEAD 4.43-4.66 / MINE 4.67-4.90. P4 HEAD 7.45-8.40 / MINE 7.88-8.28. Every interval overlaps; there is no frame-time signal to record. Three further interleaved pairs once the host quietened settle it: T0 HEAD 2.88 / 3.15 / 3.02 against MINE 2.84 / 3.17 / 2.97, T2 HEAD 5.62 / 5.52 / 5.84 against MINE 5.54 / 5.59 / 5.61 — pairwise indistinguishable, and all three modified runs GREEN on every check. Draw calls were IDENTICAL on all fifteen runs across both hosts and both checkouts, which is what a structural number should be. The lesson for whoever reads a red T0 next: this container reached 6.16 ms on a CLEAN checkout an hour before it reached 2.88 ms on the same one.\n\nP2: 38 -> 40 (P6-T07), and it is two separate things.\n(1) +1, the `ghost` overlay — the build placement preview. `PerfScene.render` passed `null` for the ghost on every frame this project has ever measured, so `pushGhost` had never executed in a gated run: the fifth time this harness silently did not measure what a phase shipped, and the largest of the five, because build mode is where a player spends the opening minutes of a match. Isolated at +1 rather than asserted: measured 38 -> 39 on the old camera path and 39 -> 40 on the new one. It is +1 and not +2 because P2's peak frame already carries `factory|0|0` for the player, so the ghost's own batch is free THERE; on frames where every player factory is past the LOD distance it does cost a second call, and those frames are not the peak.\nThe scene also drives `pickGround` and `checkPlacement` per frame from the viewport centre, as `Game.updateGhost` does from the pointer, and brings the power field up with the ghost as `Game` does (ADR-0012 §2). Six power uploads across 600 frames — version-gated, and `judge` now fails a run that uploads once per frame. None of those three had ever run inside a gate either.\n(2) +1 from the yaw sweep: a building batch that the two-snap window never had in frame. Measured by holding the ghost out — 39 on the new path, 38 on the old."
  },
  "P3": {
    "p95": null,
    "maxDrawCalls": 46,
    "recorded": "2026-08-15",
    "reason": "PRD §5's Phase 3 combat scene, and the gate ADR-0016 named as its own supersede trigger. Every unit type on the field at once, both sides packed into weapons range so shots and deaths actually happen — the other scenes use the four-type MVP mix, which meant none of ADR-0016's nine new silhouettes had ever reached a perf gate. 43 draw calls: 39 batches (15 unit meshes x 2 owners, plus buildings, imposters and a deposit), terrain, and four overlay layers. p95 is NULL on purpose: it has not been recorded on a machine worth trusting. This container reports figures 1.7x the CI runner's on identical commits, and writing one in would arm the regression check against noise. The scene is still gated on the PRD's own 33 ms budget, which is machine-independent, and on its draw-call count, which is structural. Record a p95 from a CI run before relying on the regression half.\n\n43 -> 44 (P3-T10): three ARMED Helium Bombs, which is the bomb overlay reaching a gate for the first time. `unitTypes: \"all\"` already put a Helium Bomb on this field, but an unarmed one draws nothing, so the layer Phase 3 added had never fired in any gated run — the third time this harness has silently not measured what a task shipped. Exactly +1, and that is the point of arming them HERE rather than on T0/T2: this scene already counts the bomb's own mesh for both owners, so the delta isolates the overlay instead of bundling two mesh batches in with it. The bombs sit on the map's edges and their survival is asserted every tick: any hit on an armed bomb detonates it for 3 000 damage across a 190 radius, `maxDrawCalls` is a MAXIMUM so a scene that erased itself halfway through would still report the peak it reached first, and the run would come out green while two thirds of its frames measured an empty map.\n\n44 -> 44, UNCHANGED by P5-T15, and the zero is the interesting part. This scene draws the `impact` layer on 574 of its frames — more than any other, since it is the only packed one — but the frame that carries its peak batch count is not one of them, so the maximum did not move. Left at 44 rather than nudged to 45 for headroom: a baseline is a measurement, and 44 is what this scene measures. The impact layer IS gated, on T0, T2, P2 and P4, each of which moved by exactly +1.\n\nP6-T07 — the camera path now sweeps all EIGHT of the rig's yaw snaps inside one gate run, and the structural maxima now include the warm-up frames. Both change what the gate SEES rather than what the frame costs, so both move draw calls and neither moves p95.\n(a) The path held one yaw for six seconds against a ten-second gate, so every gated measurement this project has ever recorded looked from snaps 0 and 1 and never from the other six. `YAW_HOLD_SECONDS` is 1.25 now: 8 x 1.25 = one revolution per run.\n(b) `runScene` dropped the warm-up frames' draw-call counts along with their timings. A batch count has no warm-up — it is the same on the first frame as on the ten-thousandth, on every machine — so that was not noise removal, it was the first tenth of the camera path being excluded from the only check in this gate that is machine-independent.\np95 unchanged everywhere, and measured rather than assumed: six interleaved gate runs, three of an UNMODIFIED HEAD exported with `git archive` and three of the working tree, alternating so host drift hits both. T0 p95 HEAD 2.76 / 6.16 / 5.16 against MINE 3.02 / 3.93 / 5.10 — the CLEAN checkout blew the 4.27 ms regression ceiling twice and the modified one once, which is the judge's own message playing out exactly as written. T2 HEAD 5.33-5.83 / MINE 5.75-5.78. P2 HEAD 22.04-23.91 / MINE 21.46-22.31. P3 HEAD 4.43-4.66 / MINE 4.67-4.90. P4 HEAD 7.45-8.40 / MINE 7.88-8.28. Every interval overlaps; there is no frame-time signal to record. Three further interleaved pairs once the host quietened settle it: T0 HEAD 2.88 / 3.15 / 3.02 against MINE 2.84 / 3.17 / 2.97, T2 HEAD 5.62 / 5.52 / 5.84 against MINE 5.54 / 5.59 / 5.61 — pairwise indistinguishable, and all three modified runs GREEN on every check. Draw calls were IDENTICAL on all fifteen runs across both hosts and both checkouts, which is what a structural number should be. The lesson for whoever reads a red T0 next: this container reached 6.16 ms on a CLEAN checkout an hour before it reached 2.88 ms on the same one.\n\nP3: 44 -> 46 (P6-T07), and the first of the two is the gate finding its own blind spot.\n(1) +1 from counting the warm-up frames. P3's busiest batch frame is frame 2, and the gate's 60-frame warm-up covered it — so `perf/baseline.json` has been recording 44 for a scene that draws 45. Measured on the old camera path with only this change: measured-window 44, all-frames 45. The previous entry below records leaving this scene at 44 rather than nudging it to 45, and that decision was right on the evidence available; the evidence was incomplete, and the missing 1 was never a real headroom question but a frame the harness was not looking at.\n(2) +1, `chevron`, from the new `veterans: 24` — units given upstream's own 3/8/18 kill counts so all three ranks draw, since the overlay emits one chevron per rank. P3 is the combat scene and veterancy is a combat cue. Measured on its own: 45 without the knob, 46 with it, on both camera paths.\nThe yaw sweep adds nothing to this scene's maximum — it was 46 on either path — it only makes the measured window reach the same peak the warm-up already held."
  },
  "P4": {
    "p95": null,
    "maxDrawCalls": 23,
    "recorded": "2026-08-15",
    "reason": "PRD §5's Phase 4 exit criterion, and the one row of this phase that is a MEASUREMENT rather than a build (P4-T10): `WorldBridge.step` has called `stepGalaxy` rather than `tick` since Phase 1, on purpose, so the background simulation was already running — what never existed was a number for it. The scene is T0's spec with `settledRoster` added and nothing else, so the two rows of `npm run perf` output can be read against each other and the whole difference between them is the ten settled worlds.\n\n22 draw calls, IDENTICAL to T0's, and that is the assertion rather than an accident: the background galaxy is simulated and never rendered, so a settled roster that moved this number would mean a colony had leaked into the seat's frame. It is also why a background regression cannot hide behind the structural half of this gate — the batch count is blind to it by construction, and the frame p95 and the per-world report are the only things watching.\n\np95 is NULL for the same reason P3's is, and the reason has not improved: this container reports figures around 1.7x the CI runner's on identical commits. Measured here rather than assumed — six runs of an UNMODIFIED HEAD checkout, exported with `git archive` so nothing local could reach it, failed T2's regression band every time (7.1-10.0 ms against a 5.16 ms baseline), failed T0's on three of six (up to 8.6 ms against 2.77), and straddled P2's 33 ms budget (31.9-37.9 ms). Recording a p95 from here would write this machine's noise into the repo and arm the regression check against it, which is precisely what `judge`'s own message tells the reader not to do. The scene is still gated on the PRD's own 33 ms budget, which is machine-independent. Record a p95 from a CI run before relying on the regression half.\n\nWhat this container measured, for the next phase to plan against rather than to trust as a ceiling — five runs, and the entity counts are identical in all of them because the scene is deterministic: ten settled worlds at 281 entities each cost 2.2-2.4 ms per background world-step. The round-robin's period is 4 galaxy ticks (MEASURED off the gaps between each world's advances, not imported from `BG_STEP` — a harness that hard-coded 4 would keep reporting a tidy four-phase cycle after upstream changed it), so that is 0.18-0.20 ms per world per rendered frame amortised and 1.8-2.0 ms for all ten. The number that actually has to fit under the budget is neither of those: it is 6.6-7.3 ms, the lump the three worlds of the busiest phase land on the one frame that carries their galaxy tick. Frame p95 10.0-11.5 ms against 33 ms.\n\n22 -> 23 (P5-T15): the `impact` overlay, +1, and STILL identical to T0's — which is the assertion this entry has always made. The settled roster did not add a second one, so no colony's combat has leaked into the seat's frame.\n\nP6-T07 — the camera path now sweeps all EIGHT of the rig's yaw snaps inside one gate run, and the structural maxima now include the warm-up frames. Both change what the gate SEES rather than what the frame costs, so both move draw calls and neither moves p95.\n(a) The path held one yaw for six seconds against a ten-second gate, so every gated measurement this project has ever recorded looked from snaps 0 and 1 and never from the other six. `YAW_HOLD_SECONDS` is 1.25 now: 8 x 1.25 = one revolution per run.\n(b) `runScene` dropped the warm-up frames' draw-call counts along with their timings. A batch count has no warm-up — it is the same on the first frame as on the ten-thousandth, on every machine — so that was not noise removal, it was the first tenth of the camera path being excluded from the only check in this gate that is machine-independent.\np95 unchanged everywhere, and measured rather than assumed: six interleaved gate runs, three of an UNMODIFIED HEAD exported with `git archive` and three of the working tree, alternating so host drift hits both. T0 p95 HEAD 2.76 / 6.16 / 5.16 against MINE 3.02 / 3.93 / 5.10 — the CLEAN checkout blew the 4.27 ms regression ceiling twice and the modified one once, which is the judge's own message playing out exactly as written. T2 HEAD 5.33-5.83 / MINE 5.75-5.78. P2 HEAD 22.04-23.91 / MINE 21.46-22.31. P3 HEAD 4.43-4.66 / MINE 4.67-4.90. P4 HEAD 7.45-8.40 / MINE 7.88-8.28. Every interval overlaps; there is no frame-time signal to record. Three further interleaved pairs once the host quietened settle it: T0 HEAD 2.88 / 3.15 / 3.02 against MINE 2.84 / 3.17 / 2.97, T2 HEAD 5.62 / 5.52 / 5.84 against MINE 5.54 / 5.59 / 5.61 — pairwise indistinguishable, and all three modified runs GREEN on every check. Draw calls were IDENTICAL on all fifteen runs across both hosts and both checkouts, which is what a structural number should be. The lesson for whoever reads a red T0 next: this container reached 6.16 ms on a CLEAN checkout an hour before it reached 2.88 ms on the same one.\n\nP4: 23 -> 23, UNCHANGED, and STILL identical to T0's — the assertion this entry has always made. Neither the yaw sweep nor the warm-up counting moved it, and no colony has leaked into the seat's frame. P4 takes none of P6-T07's new scene knobs on purpose: its whole design is to be T0's spec with exactly one field added, and a knob here would break the subtraction the two rows exist to support."
  }
}
