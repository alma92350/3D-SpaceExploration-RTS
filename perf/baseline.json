{
  "T0": {
    "p95": 2.77,
    "maxDrawCalls": 17,
    "recorded": "2026-08-15",
    "reason": "Draw calls 16 -> 17 for P3-T06's combat-feedback tracers. Re-recorded deliberately, per judge()'s own rule, and ONLY the draw-call count: p95 is untouched because nothing got slower. The +1 is one `tracer` overlay layer for the whole frame \u2014 measured at 67 tracers live in this scene, drawn in a single call. NOTE: this scene also exercises a second new kind, `blast`, and it never fires here \u2014 `PerfScene` extracts with its own extractor AFTER `bridge.step` has already drained `state.events`, so deaths are structurally invisible to the harness. Tracers are gated by the perf run; death marks are NOT, and P3-T16 owns closing that."
  },
  "T2": {
    "p95": 5.16,
    "maxDrawCalls": 19,
    "recorded": "2026-08-15",
    "reason": "Draw calls 18 -> 19 for P3-T06's combat-feedback tracers \u2014 the same single `tracer` overlay layer as T0, with 246 tracers live at peak in this scene and still one draw call. p95 deliberately NOT re-recorded: the CI runner measured 5.10 ms against this 5.16 ms baseline while the local host reports 8+ ms on identical commits, which is the machine artefact judge() warns about."
  }
}
