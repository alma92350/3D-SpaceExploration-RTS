# Task board

**This file is the project's memory.** It is updated in the same commit as the work it describes.
How to pick up a task: [`WORKING-AGREEMENT.md`](WORKING-AGREEMENT.md). What the phases mean:
[`../PRD.md`](../PRD.md) §5.

**Status values:** `READY` (dependencies met, take it) · `IN-PROGRESS` (claimed — see Notes) ·
`BLOCKED` (say why in Notes) · `DONE` · `PARTIAL` (shipped, but one acceptance line cannot be met
here — say which, and why) · `PARKED` (deliberately deferred).

**Claiming:** set `IN-PROGRESS` + your identifier and date in Notes, in a commit, before writing
code.

---

## Phase 0 — Foundation

Goal: an empty but trustworthy repo. Exit criteria: PRD §5, Phase 0.

| ID | Task | Status | Deps | Definition of done | Notes |
|---|---|---|---|---|---|
| P1-T01 | Bridge: create a single Odyssey world from the vendored engine and step it from the loop | DONE | P0-T05, P0-T10 | Test: 100 steps of the bridge equal 100 direct `stepGalaxy` calls, state hash identical | The two runs must be **sequential, never interleaved**: the engine mints entity ids from a module-global counter that `createGameState` resets, so two galaxies alive in one process reset each other's id sequence and diverge in positions. A real constraint on Phase 4's background worlds |
| P1-T02 | Snapshot extraction into preallocated typed arrays (units, buildings, nodes) + version counters | DONE | P1-T01 | Test: snapshot matches engine state for a hand-built world; 600 frames allocate nothing; growth is power-of-two and off-frame | Fog is applied **here**, not in the renderer: an entity the player cannot see is absent from the snapshot entirely (F-02, F-06) |
| P1-T03 | Interpolation: prev/current tick blending, spawn and death at tick boundaries | DONE | P1-T02 | Test: a unit moving at constant speed interpolates linearly; a unit spawned this tick does not appear at the origin; a dead unit does not linger | Alpha is clamped, never extrapolated — extrapolation overshoots a unit through the wall it just stopped at, on exactly the frames that ran long |
| P1-T04 | `elevation(x, y)` from the terrain grid, with cell-boundary smoothing (ADR-0004) | DONE | P1-T02 | Test: all three terrain values, cell boundaries, map edges, determinism (same input → same output) | Bilinear across cell **centres** with a narrowed blend band, so a plateau top stays flat and a cliff still reads as a cliff |
| P1-T05 | Terrain mesh: one merged static mesh from the elevation field, rebuilt only on change | DONE | P1-T04, P0-T08 | Test: rebuild count is 1 across 600 frames; vertex count within budget; T0 collapses to the flat variant | ~8k triangles for the whole ground at MVP map size |
| P1-T06 | Procedural low-poly meshes for the MVP roster (worker, skiff, bastion, lancer, command, barracks, habitat, turret, refinery) | DONE | P0-T08 | Each generator is deterministic, budgeted (tri-count assertion per mesh), and readable at MVP camera distance (playtest) | **Ten** meshes, not nine: the colony ship is the first thing a player ever sees (ADR-0010 §4). Readability is still open — it needs the playtest (P1-T24) |
| P1-T07 | Tier configuration (T0–T3) as data + auto-detection + measured correction + settings override | DONE | P0-T06 | Test: SwiftShader/llvmpipe renderer strings select T0; a stubbed 3-second budget miss drops a tier once and notifies; override persists | The measurement window is **time-based, not frame-count-based**: a count-based window let one 800 ms hitch drop a tier on a fast machine ~4 s later, and made the drop unreachable on a machine slow enough to need it |
| P1-T08 | Renderer conformance suite; WebGL implementation passing it | DONE | P0-T08 | Same suite green against `RecordingRenderer` and `WebGLRenderer` | Suite runs in both places: Node against the fake (fast, in the red-green loop) and Chromium against all three implementations including Canvas2D. Caught a real Canvas2D defect — it reported zero draw calls for the terrain before the first `setFog` |
| P1-T09 | Instanced entity rendering: one draw call per (mesh, owner) | DONE | P1-T06, P1-T08 | Test: 200 units of 4 types across 2 owners ⇒ ≤ 8 instanced draw calls; `FrameStats` proves it | Measured: 15 draw calls total at the T0 perf scene (200 units, 80 buildings) including terrain, nodes and every overlay |
| P1-T10 | RTS camera rig: pan, zoom-with-pitch, yaw (Q-01), bounds clamping, focus-base | DONE | P0-T10 | Test: camera never leaves bounds at any zoom/yaw; focus centres the target; pure math, no DOM | Q-01 answered in ADR-0010: yaw snaps to 8. Clearance is checked under the **eye**, not under the target — the camera flies over mesas on its way somewhere else |
| P1-T11 | Input → intent translation: click, box select, double-click type-select, move/attack-move/stop/hold, hotkeys | DONE | P1-T10 | Test: each gesture produces exactly one intent with the right payload; no intent writes sim state directly | Hotkeys are upstream's letter for letter (ADR-0010 §5) — stop is `X`, not `S`, because W/A/S/D pan |
| P1-T12 | Ray → ground picking against the elevation field, and entity picking | DONE | P1-T04, P1-T10 | Test: picked `(x, y)` within ±0.5 world units of truth across a matrix of yaw × pitch × zoom × terrain type (PRD F-03) | Worst measured error across the matrix is far inside ±0.5. Two tests, because a world round-trip alone cannot distinguish a picking bug from correct occlusion: the second sweeps every on-screen pixel and demands it round-trip to within one pixel |
| P1-T13 | Architecture tests: no `view/**` → `engine/**` import; no `z`/elevation field crossing the bridge | DONE | P1-T02 | A fixture violating either rule fails the test | Plus: only `src/engine/index.ts` may import vendored JS, and in the render path a typed array may only be allocated in a constructor, a field initialiser or an `ensure*` helper |
| P1-T14 | Fog of war rendering: three states, single low-res lookup, no per-entity branching | DONE | P1-T05, P1-T09 | Test: hidden entities are absent from the snapshot, not merely undrawn; fog texture updates once per tick, not per frame | One texture fetch in the terrain shader; the version counter is asserted by the conformance suite |
| P1-T15 | Selection, health bars, veterancy chevrons and rally lines as overlays | DONE | P1-T09 | Conformance test: overlays render in both WebGL and Canvas2D implementations; legible at min zoom (playtest) | Drawn as projected 2D over the scene in both implementations, so they keep a constant pixel size. Legibility still needs the playtest |
| P1-T16 | HUD: resource bar, selection panel, production buttons, supply, alerts (plain DOM) | DONE | P1-T02 | Test: panel content is a pure function of a snapshot; every number matches the engine's own value | `hudModel(snapshot)` is pure and tested without a DOM; the DOM writer only touches nodes whose text changed |
| P1-T17 | Minimap with terrain, fog, entities and click-to-move-camera | DONE | P1-T05, P1-T14 | Test: minimap→world coordinate conversion round-trips; redraw cost inside budget | Fog layer is repainted only when `fog.version` moves, then blitted |
| P1-T18 | Build placement: 3D ghost, validity shading (terrain, collision, build reach), commit | DONE | P1-T12, P1-T16 | Test: validity matches the engine's own placement rules exactly, on every terrain type | Verified by sweeping the whole map and comparing verdict-for-verdict against `canPlaceBuilding` + `sampleTerrain`. Validity is shape **and** colour (dashed + crossed when invalid), never colour alone (N-05) |
| P1-T19 | Canvas2D fallback renderer, feature-reduced but playable | DONE | P1-T08 | Conformance suite green; the no-WebGL boot test reaches a playable frame | Projects through the same camera matrix as the WebGL path, painter-sorted. Ground is drawn as fog-grid cells rather than the merged mesh |
| P1-T20 | Save/load of the single world (upstream `GALAXY_SAVE_VERSION` parity) | DONE | P1-T01 | Test: a save written here loads in the 2D client's format checker and round-trips to an identical state hash | Uses upstream's own `serializeGalaxy`/`deserializeGalaxy`. A corrupt save is rejected without touching the running session — upstream signals failure two ways (null **and** a throw), and both had to be caught |
| P1-T21 | Determinism fixtures: recorded seed + command stream → committed end-state hash | DONE | P1-T11 | Replay is bit-identical in CI and on two developer machines; a deliberate engine tweak fails it | The fixture is **generated** by `test/determinism/record.test.ts` (`RECORD_FIXTURE=1`), not hand-written: engine ids depend on *when* an order is issued, so a hand-written fixture hashes deterministically while every order in it silently fails. Still needs confirming on a second and third machine (see the gate note below) |
| P1-T22 | No-WebGL boot path test (context creation stubbed to fail) | DONE | P1-T19 | App starts, selects Canvas2D, reaches a playable frame, no uncaught error | Asserts pixels were actually drawn, not merely that boot completed |
| P1-T23 | Perf gate at MVP content: T0 200 units @ 33 ms, T2 400 units @ 16.6 ms | PARKED | P1-T09, P1-T14 | `npm run perf` green at both tiers on the committed scene; baseline recorded | **T0 passes: p95 12.7 ms against 33 ms**, under SwiftShader, which *is* the T0 target. **T2's budget cannot be gated in CI** — it is defined as "integrated GPU ≥ 2019" and CI has no GPU at all, so the T2 scene is run for its hardware-independent contracts and its frame time is recorded, not asserted. See the gate note below **Reclassified by P7-T04, having read PARTIAL for six phases.** The open half is not pending work: T2's budget is defined as "integrated GPU >= 2019" and CI has no GPU at all, so no amount of effort in this repo closes it. T0 passes at p95 12.7 ms against 33 under SwiftShader, which IS the T0 target. **Trigger to reopen: a CI runner with a GPU**, at which point T2's frame time becomes an assertion instead of a recording. PARTIAL implied someone would finish it; PARKED says what would have to change first. |
| P1-T25 | First-run readability: the opening must not read as a blank page | DONE | P1-T16 | The first frame shows a legible colony ship, ground rather than void, and one line saying what to do | Raised by looking at the built app. Four causes, all fixed: the camera opened at 420 on a single small ship; unexplored terrain rendered pure black over ~90% of the view; the map edge ended in a hard black wedge; nothing said what to click. **Scope change: the starfield in PRD §5 is cut** — the camera's pitch ramp means the horizon is never on screen, so a skybox is invisible by construction. A dark terrain apron replaces it (ADR-0010 §5–6) |
| P1-T26 | CI: the browser job could never start | DONE | P0-T02 | `npm run smoke` reaches the tests on a GitHub runner | `vite preview` binds `localhost`, which resolves to `::1` on a dual-stack runner while Playwright polled `127.0.0.1` — three minutes of webServer timeout with no test ever running. Binds explicitly now. Also stopped reusing an existing preview server: a stale one silently serves an old `dist/`, so the browser suite tests a build that no longer matches the source |
| P1-T27 | Publish the build to GitHub Pages | DONE | P0-T02 | A public URL serves the playable game; the build works from a subpath | Pages had been pointed at a *branch*, which publishes Vite source and yields an unstyled "Loading the Odyssey…" that never boots. Source must be **GitHub Actions**. `base` is `"./"` so one artefact works at both `/` and `/<repo>/` — no deploy-only build path for nobody to test — and `e2e/subpath.spec.ts` serves the real build from a nested path so a rooted asset URL fails CI rather than the public site |
| P1-T28 | S5 measured rather than inferred: cold load to interactive under a throttled connection | DONE | P1-T16 | `npm run smoke` fails if a cold load at 10 Mbit reaches a playable frame later than 5 s | S5 had been called a pass by arithmetic — 192 kB at 10 Mbit is 0.15 s, therefore fine — which ignores everything between the last byte and a playable frame: parse, compile, GL context creation, mesh generation, the terrain build, the first draw. On a machine with no GPU that is where the time actually is. `performance.mark("odyssey:interactive")` is set from the entry point after the first frame returns (§4.2 asks for `performance` marks), and CDP throttles the link. **Measured: 331 ms against 5 000 ms.** The second test in the file is the important one — it re-runs the load unthrottled and fails if the two numbers match, because a throttling harness that silently fails open reports a comfortable number forever |
| P1-T29 | The perf gate's regression message pointed at the one action that disables the gate | DONE | P1-T23 | `judge()` has tests; a regression message sends the reader to re-measure on a clean checkout before it mentions the baseline | Found by hitting it: this container measured T2 p95 **7.16 ms against a 5.16 ms baseline it had recorded itself hours earlier**, with no code change and CI green on the same commit — host load, not a regression. The old message's only advice was "re-record the baseline", and following it would have written a loaded machine's number into the repo, after which the gate passes forever and detects nothing. Message fixed and pinned; `judge()` had no tests at all before this, so its four real rules got them too. **The baseline was deliberately NOT re-recorded** |
| P1-T24 | Playtest script `docs/playtests/mvp.md` + one recorded playtest against S1/S6 | IN-PROGRESS | P1-T18 | Script exists; 3 of 5 testers find the build menu unaided, or a follow-up task is filed | Script written and updated for the built MVP (world, controls, tier switching). **The five-tester session has not been run** — it needs people, not code |

---

## Phase 1 exit gate — status

Checked against PRD §5 (Phase 1) and §4.2. Two criteria are **not** met, and neither can be met by
writing more code.

| Criterion | Status | Evidence |
|---|---|---|
| S1 — land, build a Command Center, train units, destroy something | **Automated ✔, human ✘** | The whole chain runs end to end in `e2e/smoke.spec.ts` and in the determinism fixture. The *judgement* S1 asks for is a playtest (P1-T24) |
| S2 — 30 fps at 1280×720, 200 units, software rendering | **PASS** | p95 **12.7 ms** against a 33 ms budget, Chromium forced to SwiftShader (`e2e/perf.spec.ts`) |
| S3 — 60 fps at 1600×900, 400 units, integrated GPU ≥ 2019 | **NOT VERIFIABLE IN CI** | CI has no GPU. The T2 scene's frame time is measured and printed but not asserted; S3 needs a run on real 2019-or-later integrated hardware before the gate can be called green |
| S4 — bit-identical simulation for the same seed and inputs | **PASS in CI, unconfirmed across machines** | `test/determinism/replay.test.ts`. The PRD asks for three machines; this is one |
| S5 — cold load to interactive ≤ 5 s on 10 Mbit | **PASS, measured** | **331 ms** to a drawn frame with the link throttled to 10 Mbit and the HTTP cache disabled (`e2e/coldload.spec.ts`, P1-T28). Was previously a pass by arithmetic from the payload size, which measured none of the parse/compile/GL-init/first-draw time that actually dominates it |
| S6 — a first-time player finds the build menu unaided | **NOT MEASURED** | Needs P1-T24's five testers |

**Scope deviations from PRD §5, both recorded in ADR-0010:** the MVP world is Helix Belt rather
than the illustrative `ferros`; and the starfield skybox is cut, because the camera can never see
the sky. The PRD should be amended for both.

## Deferred verification

Criteria that CI cannot decide, kept in one place so a closed phase never quietly implies they
passed (ADR-0011). This list only grows until someone runs the checks.

| Criterion | Phase | Needs | Deferred because |
|---|---|---|---|
| S1 — the chain judged by a person | 1 | five playtesters | Automated end to end in `e2e/smoke.spec.ts`; the *judgement* is the point and needs people |
| S6 — a first-time player finds the build menu unaided | 1 | five playtesters | The one criterion that asks whether the game is legible rather than fast |
| S3 — 60 fps at 1600×900, 400 units | 1 | a 2019-or-later integrated GPU | CI has no GPU. **Deferred indefinitely**, not merely delayed: CPU-only is the whole target |
| S4 on a second and third machine | 1 | two more machines | Reproducible in CI; the PRD asks for three |
| Mesh and overlay legibility | 1, 2 | playtesters | The silhouette tests fail what a person would obviously fail, which is not the same as reading well |
| "Every combat cue is legible in a blind readability test" | 3 | playtesters | Same shape as S6, and it will be deferred the same way |
| A 20-minute AI-vs-player match at budget | 3 | a soak run | P3-T16 gates a representative combat frame at the budgeted counts. A 20-minute match is a soak test whose entity counts drift as units die — the non-determinism the fixed-population harness exists to avoid — so it needs its own shape, not a longer run of this one |
| A trusted p95 for the `P3` combat scene | 3 | one CI run | Recorded as `null` rather than filled in from this container, which reports 1.7x the runner's figures on identical commits. The scene is gated on the PRD budget and its draw-call count until someone records a real one |

| Phase 2's panels reachable by a player | 2 | a decision, then code | Six of the seven panel modules are imported by nothing. Not a playtest gap — a code gap, and the only entry in this table that CI could close. See "The reachability gap" under Phase 3 |

**What the next session should pick up, in order:**

1. ~~Decide the reachability question~~ — **done, as P4-T01**: the panels are wired, both phases
   carry an enumeration test, and all three playtest scripts are now runnable end to end.
2. **P1-T24 and P3-T18 in one sitting** — run the three playtest scripts with five people, now that
   there is a URL: https://alma92350.github.io/3D-SpaceExploration-RTS/ They gate S1, S6 and Phase 3's
   own legibility criterion, and they are the only things that can say whether the meshes and
   overlays actually read. `mvp.md`, then `economy.md`, then `combat.md` — the last two in the same
   sitting, for the reason `combat.md` states at the top. Everything else on this board is green
   without them and means less than it looks.
3. **S3 on real hardware** — one run of `npm run smoke` on a laptop with a 2019-or-later integrated
   GPU, with the T2 assertion turned back on locally. File what it says.
4. **S4 on two more machines, and a trusted `P3` p95** — `npx vitest run test/determinism` elsewhere
   and compare the hash; and record `P3`'s p95 from one CI run, since this container reports 1.7×
   the runner's figures on identical commits.

Every remaining item needs a person or a machine this project does not have in CI. The one that did
not — reachability — is closed, which means the playtest scripts are now the only thing standing
between this board and an honest answer about whether any of it reads.

## Phase 2 — The economy

Goal: the full per-world build/economy loop that Odyssey actually runs on. Exit criteria: PRD §5,
Phase 2 — *a player can run the full 2D economy in 3D with no panel missing; every economy panel has
a logic test; perf budgets hold with 300 buildings on screen.*

> **Phase 1's gate is closed on its automated criteria (ADR-0011).** S1, S3 and S6 are deferred —
> they need five playtesters and a GPU, not code — so Phase 2 is under way rather than waiting.
> Rows still marked `BLOCKED` are blocked on their `Deps`, nothing more.

**What the MVP taught us, which is why this decomposition looks like it does.** The numbers below
are from the vendored engine, not from the PRD's prose:

- **29 buildings and 23 commodities.** The MVP renders 9 building types and shows 3 commodities plus
  credits. Phase 2 is mostly *breadth*: 20 more building types, 20 more commodities, and the panels
  to read them. That is a different kind of work from Phase 1's, and it is why the perf risk is
  draw calls and snapshot width rather than any single clever system.
- **The industrial chain is 9 recipes, not 7.** `recipeOf` covers smelter, assembler, chipfab,
  machineworks, antimatterforge, aifoundry, torpedoworks, chemplant and fabricator — PRD §5's arrow
  diagram omits the Chemical Plant and the Fabricator, and both are on the consumer-goods path.
  Four recipes are tech-gated (`metallurgy`, `electronics`, `antimatter`, `aicores`), so the
  research UI (P2-T11) is a dependency of the chain reading correctly, not a side panel.
- **Power is a distance field, not a boolean.** `POWER_TIERS` bands by range — on-grid ≤ 190,
  near ≤ 320, far ≤ 470, isolated beyond — each with a cost multiplier (1×, 1.3×, 1.7×, 2.3×). It
  is the same shape of problem as fog, which is already solved as one low-res texture with a version
  counter, and Q-08 asks whether to reuse that machinery.

| ID | Task | Status | Deps | Definition of done | Notes |
|---|---|---|---|---|---|
| P2-T01 | Snapshot: the owner's full commodity stockpile, supply and power totals | DONE | P1-T02 | Every commodity in `COM` (23) reaches the view with the engine's own value; 600 frames allocate nothing; growth stays power-of-two and off-frame | The MVP carries ore/crystals/radioactives/credits. Widening the stockpile is cheap — it is per-owner, not per-entity — and it is the dependency of every panel below |
| P2-T02 | Snapshot: per-building production state (recipe, progress, input buffers, output buffer, throttle reason) | DONE | P2-T01 | Matches `recipeOf` + the engine's building fields for a hand-built world of every producing type; no per-frame allocation | **Answer Q-07 first.** 23 commodities × 300 buildings is 6 900 floats a tick if every buffer crosses every tick; the alternative is totals for all, detail for the selection only |
| P2-T03 | Bridge commands: research, recycle, repair, market buy/sell, doctrine, logistics priority | DONE | P2-T01 | Each intent produces exactly one engine call and is refused exactly when the engine refuses it — asserted against the engine's own predicate, not a copy of its rules | The MVP's rule: the bridge never re-implements a rule it can ask about (`canPlaceBuilding` is the precedent). `researchTech`, `beginRecycle`/`canRecycle`, `buy`/`sell`, `LOGI_PRIORITIES` |
| P2-T04 | Meshes for the remaining 19 building types | DONE | P1-T06 | Every engine building type maps to a built mesh; each mesh inside its triangle budget; the families distinguishable from each other by profile | **Scope changed by ADR-0013**, which supersedes ADR-0012 §3's variant attribute: that mechanism would have put all nine rooflines in the vertex buffer and collapsed eight per instance — 36k triangles of vertex work at 300 buildings against a scene that runs 13k in total, spending more budget than the draw calls it saved. Six families instead, 14 building meshes, 28 draws at two owners. **Nine chain buildings now look identical on the field** — a bigger loss than ADR-0012 admitted, and it belongs to the deferred playtest. The profile test earned itself immediately: it failed the first Fortress for coming within 8% of the Refinery |
| P2-T05 | Logistics unit meshes + visible cargo | DONE | P2-T04 | `hauler`, `heavyhauler`, `bulkfreighter` and `freighter` render; a laden unit is distinguishable from an empty one; **cargo adds no draw call** | **One hull, not four** (ADR-0014). Cargo rides the per-instance `shade` the batch already carries — a second "laden" mesh would have cost a draw call per owner to say one number. The test is a *pair*: the laden unit must look different AND the batch count must not change, because either assertion alone would pass the wrong implementation. Fullness rather than a boolean, so a hauler on its way home does not read like one that has barely started. **The shade goes DOWN, not up** — brightening past 1 was the first instinct and would have put 1.35 into a channel the port documents as 0..1, which nothing asserts, so it would have survived: WebGL brightens, Canvas2D might clamp, and the two renderers would disagree about the same frame. (This row was corrupted by a scripted edit that overwrote the task name with a status; restored by hand.) |
| P2-T06 | Power / electrification zones rendered | DONE | P1-T14 | The four `POWER_TIERS` bands render in every implementation; one extra texture fetch, no per-entity branching; uploaded once per tick behind a version counter, asserted by the conformance suite | Q-08's answer held: reusing the fog machinery made this small. `setPower(field \| null)` — one method, null hides — with the texture kept so toggling costs nothing, and a conformance case that fails if hiding counts as an upload. **It is a placement cue first**, on automatically while a build ghost is up (the moment "will this run efficiently here?" is actually asked) and otherwise on `G`. NEAREST filtering, unlike the fog: a band edge is where the cost multiplier changes, and smoothing it would draw a gradient the engine does not have |
| P2-T07 | Gathering and hauling read correctly in 3D | DONE | P2-T05 | A recorded worker round trip — `updateGather` → `nearestGatherDrop` → drop-off — matches the visible cargo state at every tick of the replay | **The round trip found that the cargo cue was wrong for every unit it was built for.** The extractor read fullness as `min(1, qty / 10)` — 10 being the *Worker's* `cargoCap` and nothing else's. A Hauler holds 250, a Bulk Freighter 1600, so ten crates aboard read as a **full** hold: every logistics unit showed a laden cue from its first crate and never changed again, which is exactly the state P2-T05's shade was added to distinguish. Fullness is the engine's own `tripCapacity` rule now (`cargoCap ?? cargoHold`), with zero-capacity units — the scenario-spawned `freighter` defines neither — reading 0 rather than dividing by it; the number rides the instance `shade` into a vertex buffer, where one NaN corrupts a batch and not a unit. **P2-T05's own test agreed with the bug and stayed green**: it used 10 for "full" because the extractor did, so both halves were wrong together. It reads `UNITS.hauler.cargoHold` now. The trip is compared **every tick, not sampled** — the deposit is instantaneous (`updateGather` banks the load and flips the phase inside one tick), so sampling could step over the only frame where a full worker and an empty one are adjacent. The drop-off is `nearestGatherDrop`'s own answer rather than a re-derived "nearest Command Center": a landed collection-point freighter can win it, and buildings are scanned before units so an equidistant building takes the tie — a re-implementation would agree on the one-base opening and diverge on the cases the rule exists for |
| P2-T08 | Building state reads without colour: constructing, working, idle, throttled, unpowered | DONE | P2-T02 | Each of the five states is distinguishable by shape or motion in a still frame; conformance suite covers both renderer implementations | **ADR-0015: the badge is a SHAPE vocabulary behind a new `status` overlay kind.** There is no per-instance colour channel to abuse even if we wanted one — colour comes from the batch key's `owner` — so "tint it red" is a second material per state per owner, ADR-0006's named failure. `shade` and `scale` were both already spoken for (construction P1-T09, cargo P2-T05). The buildings budget is at 28 of 28, so a per-state mesh is unavailable at any price. **The finding that shaped it: the engine cannot tell idle from working.** `buildingConcern` returns null for an idle Barracks *and* for a producing smelter — a Barracks has no recipe, so the engine has nothing to complain about — and idle-vs-unpowered is exactly the confusion this row warned about. Hence `ACTIVITY_*`, ours, in its own array rather than appended to the engine's concern enum: ADR-0012 §5 promises `concern` is `buildingConcern`'s answer untouched, and mixing ours in would make the next reader who trusts that comment wrong. Glyphs are pure data in their own module, so "these two states are distinguishable without colour" is a unit test over point sets rather than a screenshot review — which matters because the playtest that would otherwise catch it is deferred (ADR-0011). Every badge strokes in ONE colour on purpose: per-glyph colours would let a later change drop the shapes and still pass a visual review. Confusable states are opposites — starved points down, bufferFull points up; idle is a closed ring, noPower a broken bolt. **Verified by mutation**: disabling the overlay, collapsing all glyphs to one shape, and never reporting idle each fail the suite. Conformance covers WebGL2 and Canvas2D in the browser, with a dedicated case for the badge because the generic stride sweep packs 100, 107, 114… and never lands on a real concern code. Cost paid: six glyphs is a vocabulary a player must be taught |
| P2-T09 | Building detail panel: recipe, buffers, throughput, why it is stopped | DONE | P2-T02 | Pure `panelModel(snapshot, id)` tested without a DOM; every number equals the engine's; the stop reason names the actual cause (`powerThrottle`, missing input, output full) | Q-10's pattern, established here for the five panels that follow: `buildingPanelModel(snap)` is its own module, pure, tested without a DOM, and composed into `hudModel` rather than growing it. The stop reason is the engine's `buildingConcern` code turned into a sentence — six codes, six *different things to do about it*, and throttled is a warning rather than a stop because the factory is producing, just slower. Energy is dropped from the recipe text: it is a power flow, not a hauled good, and listing it sends a player looking for energy to haul |
| P2-T10 | Market panel | DONE | P2-T03 | Every price equals `unitPrice`/`quoteSell`; a trade goes through `buy`/`sell` and the panel shows the engine's result rather than its own arithmetic; `TRADE_LOT` and the glut/pressure bounds are visible, not implied | Every price is `unitPrice`'s or `quoteSell`'s. The one that matters is **`sellLotProceeds`, which is `quoteSell`'s dry run of the real lot walk, not `sellUnit × TRADE_LOT`** — `sell` slips the price between lots, so a big order earns strictly less than the multiplication says, and it is wrong by *more* the larger the order. Upstream already solved it; a panel that multiplied would be wrong exactly when the player is paying attention. Reads engine `State` rather than the snapshot: prices are read when a panel is open, not 20 times a second, and widening the snapshot for them would put 23 more numbers a tick behind a panel most players have closed |
| P2-T11 | Research / tech UI | DONE | P2-T03 | Available, queued, in-progress and locked match the engine's gating exactly, swept over all of `TECHS`; cancel returns what the engine says it returns | `canStart` is a **dry run of `researchTech` itself**, called and undone, not a copy of its six-step gating — one of those steps is "a prerequisite counts as met if it is queued ahead on this same Datacenter", which nobody would think to copy. Tested by sweeping ALL of `TECHS` and comparing against the engine node by node, because one node's gating drifting while the rest look fine shows up to the player as a button that does nothing. The dry run restores the queue **in place**: replacing the array left the caller holding a reference to the mutated one, which surfaced as a third tech in a queue of two |
| P2-T12 | Refinery doctrines UI | DONE | P2-T03 | Selecting a doctrine changes exactly what the engine changes; the panel states the trade-off in the engine's own numbers | The engine does expose a doctrine API — `UPGRADES`, `committedDoctrine`, `researchUpgrade`, `upgradeMult` — so nothing had to be invented. **`researchUpgrade`'s prerequisite rule is the exact OPPOSITE of `researchTech`'s**: a tech queued ahead on the same Datacenter satisfies its successor, a Refinery job queued ahead does *not*. Two panels, two contradictory rules, one function name apart — so `canStart` is a dry run of `researchUpgrade` itself, exactly as P2-T11's is, and a copied predicate would have been right about one panel and wrong about the other. **The trade-off this panel has to state is not the cost, it is the irreversibility.** Committing spends the other two doctrines for the rest of the match on one click, and the engine's refusal is a bare `false` — indistinguishable from "you cannot afford it". So `blockedBy` carries a third value, `"doctrine"`, meaning *never* rather than *not yet*, and `choiceIsOpen` is true only while all three paths are still live. The lock lands on a QUEUED job, not a completed one (`committedDoctrine` reads the Refinery queue), so a panel that waited for completion would offer the other two paths for 25 seconds and have every click silently refused. Effects are the engine's raw numbers verbatim — 1.15, not "+15%" — because converting is this module doing arithmetic on a balance number, and a sign error there is invisible until someone counts damage in a real fight; the sentence a player reads is the engine's own `desc`. Filtering effects by a `*Mult` suffix is the obvious implementation and **silently drops the entire Bulwark capstone** (`selfSealingPlating` grants `regenRate`/`regenDelay`), so the filter is a deny-list of bookkeeping keys instead. `hardEdge` is excluded by `.doctrine` rather than table membership — the same guard the engine's own `committedDoctrine` uses, because seeded before any real research it would otherwise read as a commitment and disable the doctrine lock for the whole match. **Verified by mutation**: the suffix filter, dropping the permanent-vs-temporary distinction, and a hand-copied tier gate each fail the suite |
| P2-T13 | Logistics panel: haulers, priorities, upkeep | DONE | P2-T03 | `LOGI_PRIORITIES` round-trip through the UI; `countLogistics` and `aiUpkeepRate` shown per owner; a priority change re-targets the next assignment, asserted on `assignHaul` | **`countLogistics` could not be used, and finding out why matters more than the panel does.** It looks exactly like the query this wants and is a *mutator* — it walks every unit's order and writes `haulers`/`servers`/`ferriers` tallies onto buildings. Calling it from a panel would have the view writing sim state on a frame: an ADR-0008 violation and a determinism bug that would appear only while a panel happened to be open. The model counts read-only instead. Counting an order is observation; `assignHaul`'s decision about which building deserves the next worker stays in the engine |
| P2-T14 | Supply and Habitats in the HUD | DONE | P2-T01 | Supply used/cap match `supplyUsed`/`supplyCap`; being supply-blocked is stated, not left to be inferred from a failed click | Closed. Being supply-blocked is now *stated* — the MVP showed "3 / 3" and let the player discover the cap by clicking a train button that did nothing. A cap of zero is a real state at the start of a match and would have put `Infinity` into a progress bar's width |
| P2-T15 | Repair and recycling UI | DONE | P2-T03 | Recycle shows `recycleValue` before committing and is cancellable; repair targeting matches `pickRepairTarget` | **Repair half closed.** `countRepairJobs` is a MUTATOR — it walks every unit's order and writes a `repairers` tally onto every building and unit — so the panel counts orders read-only instead. That is the **second** time this exact trap has appeared (`countLogistics`, P2-T13), and it is asserted now, not just commented: a test checks no `repairers` field is stamped by reading the panel. The next finding is the one that shaped the design: **Odyssey structures continuously decay** (`updateDecay`, 0.06% of max HP per second down to a 35% floor), so `hp < maxHp` is true for a player's entire base, permanently, from the first second — a panel filtering on "damaged" would list everything they own and mean nothing. The filter is the engine's own release point, `< HEALED`. That same decay makes the hysteresis band the *normal* state rather than an edge case: a building takes ~4 minutes to drift from full to `NEEDS_REPAIR` and spends all of it visibly damaged with nothing coming, which is precisely the stretch that makes Menders look indecisive. So `worn` is a state with a name — damaged, and **nothing is coming** — distinct from `healing`, where a committed repairer works on through the same band. `nothingWillBeDispatched` says it outright. Targeting is `pickRepairTarget`'s own answer, asked from the repairer's position because distance breaks ties and the answer legitimately differs per Mender; the ≤2-per-target cap is `assignRepair`'s policy and its constant is module-private upstream, so it is not copied. Recycle half (unchanged): the preview is `recycleValue`'s own number, which counts a fraction of the build cost **plus everything in the entity's buffers** — a factory holding 30 metals refunds those too, and a panel guessing from the build cost would understate a full building badly, with the player finding out only after it was gone. **Repair UI is not built**: `NEEDS_REPAIR`/`HEALED` hysteresis still needs to be visible or menders will look indecisive |
| P2-T16 | Plasma Rig survey and yield UI | DONE | P2-T03 | `rigSurvey` results render inside `SURVEY_RADIUS`; the rolled `YIELD_TIERS` tier is shown from `rigInfo`, never re-derived | **`rigSurvey` takes the node list as an argument, and that argument is a trap that compiles.** The engine's own comment says why: the sim surveys ALL nodes deterministically, while the UI must survey only the ones the player has discovered — "a hidden cache stays a surprise". Handing it `state.map.nodes` runs fine and produces a *better* reading, which is exactly the problem: it is the sim's answer, leaking every unscouted deposit on the map through a build ghost. Measured on this seed: eight spots where the two disagree, one reading **fair crystals** from all nodes and **poor ore** from what the player actually knows. The test finds such a spot rather than hardcoding one, so it cannot pass by the two coinciding. `blind` is the engine's null vein, not a low confidence — one deposit in range gives confidence 1.0 and is still a guess, so inferring one from the other would mislabel a confident single-deposit reading. The `seen` list matches `surfaceSurvey`'s strict `>=` cut, so a deposit exactly on the radius is not shown beside a number it contributed nothing to. Yield half is the opposite discipline: `lastTier` is `rollTier`'s result (rig id + dig counter, biased by richness) and is reported verbatim; the multiplier is a **lookup by name in `YIELD_TIERS`, never `lastYield / baseYield`**, which agrees today and diverges silently the first time an upgrade touches the yield. No tier before the first dig, rather than a plausible "low" that reads as a seam that was never struck. Stop reasons follow the engine's own gating order — a rig with no fuel *and* a full buffer is stopped by the fuel, and naming the buffer would send the player to do work that changes nothing. **Verified by mutation**: surveying all nodes, defaulting the multiplier, and swapping the gating order each fail the suite |
| P2-T17 | The remaining commodity/resource node meshes | DONE | P1-T06 | Ice, gas, biomass, spice and relics nodes are distinguishable from ore/crystals/radioactives at camera distance | **Two meshes, not five** (ADR-0014): `node` for the metallic and crystalline deposits, `volatile` for ice, gas, biomass, spice and relics. Three was the obvious grouping and costs a batch at *every zoom* — measured: `pushNodes` batches at `LOD_MESH` unconditionally, so unlike entities a deposit mesh never collapses to an imposter. The metallic-versus-crystalline distinction is one a player makes in the build menu, not by looking at the ground. `snap.nodes.comIndex` had crossed the bridge since Phase 1 and been discarded here; it decides the mesh now. The volatile mesh went five-sided after six put it at 36 triangles against a 30 budget — the proportion separates it from the rock, not the facet count |
| P2-T18 | Perf: the Phase 2 scene and its gate | DONE | P2-T04, P2-T06 | 300 buildings across all 29 types plus 200 units holds the T0 budget; draw calls asserted | **Both halves measured, and PRD §5's Phase 2 criterion holds.** Draw calls: 28, exactly the ceiling (`test/view/economy-draw-calls.test.ts`). Frame time on a GitHub runner: **p95 21.95 ms against 33 ms**, sim 6.59 ms/frame, 27 draw calls, 16k triangles. The scene populates from all 29 types, because 300 copies of one mesh would sail through a budget that 29 distinct types have to be collapsed to meet. **The finding worth keeping is the shape, not the pass:** p50 is 0.18 ms, so the p95 is the frames a simulation tick lands on — at 300 buildings the *engine* consumes the budget, not the view, and ADR-0006's numbers were written assuming the opposite. A third of the budget is left and it is the sim's to spend; more buildings will exhaust it, and under ADR-0003 that fix is upstream. This container measured 31.9–35.2 ms for the same scene while running well below its own recorded baseline (P1-T29) — a reminder that a local perf reading here decides nothing |
| P2-T19 | Determinism fixture extended with economy orders | DONE | P2-T03 | The recorded replay includes economy orders and hashes identically; each one provably moves the hash | Six new orders — trade, logistics priority, pause, resume, recycle, cancel-recycle — across 1 400 ticks, 20 orders, 13 intent kinds. **Two things had to be fixed before the fixture actually covered them.** (1) `hashState` hashed only physical fields, so `paused`, `logiPriority` and `recycling` could stop reaching the engine with the hash unmoved — a green fixture proving nothing about the commands it issues, one layer below the failure the recorder itself was built to prevent. The economy fields and both buffers are hashed now. (2) The recycle/cancel pair **cancels out**: with `recycle` broken, `cancelRecycle` is a no-op and the end state is byte-identical. Found by dropping each order from the fixture and replaying — `recycle` alone came back NOT COVERED — and fixed with a second, uncancelled recycle at tick 1300 that leaves the Barracks recycling at the final tick. All five now provably move the hash. **Research is deliberately absent**: it needs a Datacenter (200 ore) and crystals, and the recorder cannot inject resources because the fixture records only a seed and a script — anything handed to the player would not exist on replay. Covering it needs a longer scenario, not a special case |
| P2-T20 | Phase 2 playtest script + one recorded session | BLOCKED ON HUMANS | P2-T09 … P2-T16 | Script exists; a tester runs the full chain to machinery without help, or a follow-up task is filed | **Script written (`docs/playtests/economy.md`), UNRUN under ADR-0011** — same disposition as P1-T24. Written now rather than reconstructed later, while the reasoning behind each cue is still fresh. It is aimed at the three legibility debts Phase 2 took on to buy its draw-call budget, each recorded in an ADR that admitted the cost and deferred the measurement to exactly here: thirteen entity types sharing a silhouette (ADR-0013, ADR-0014), metallic vs crystalline deposits (ADR-0014), and six status glyphs being a *vocabulary* rather than a self-evident cue (ADR-0015). **Two starting points, and both are required**: a cold start measures discoverability, a prepared save measures whether a going concern is readable — half the script is unreachable inside an hour from cold, and a tester who never reaches the Fabricator tells us nothing about it. The prepared save carries one starved Smelter, one unpowered Assembler, one idle Barracks and a Datacenter mid-research, handed over with no explanation. Three numeric gates written in advance so a result cannot be argued into a pass afterwards: E1 the colony reads, E2 the vocabulary is learnable, E3 the silhouette debt is survivable — and E3's failure trigger is an ADR with a measurement, not a task, because the budget that forced ADR-0013 has not moved. **The fuel trap is called out as the most likely place for a session to die**: a Reactor grants nothing until fuel reaches its own larder, so a freshly built one is *correctly* dead, and the badge is the only thing that says so **Reclassified by P7-T04, alongside P6-T09.** The script is written; running it needs testers. PARTIAL suggested a remaining engineering task and there is none — this is the same disposition as every other unrun script, and collecting them under one status is what makes the size of that backlog visible rather than spread across four phases. |

## Phase 2 exit gate — status

Checked against PRD §5 (Phase 2): *"a player can run the full 2D economy in 3D with no panel
missing; every economy panel has a logic test; perf budgets hold with 300 buildings on screen."*

| Criterion | Status | Evidence |
|---|---|---|
| No panel missing | **PASS** | Eight panel models: building detail, market, research, doctrines, logistics, supply/Habitats, repair + recycling, Plasma Rig survey + yield. Every screen the 2D client's economy has |
| Every economy panel has a logic test | **PASS** | All eight are pure `(state \| snapshot) → model` functions tested without a DOM (Q-10, ADR-0012 §4). **344 unit tests**, 15 browser tests |
| Perf holds with 300 buildings | **PASS, measured** | GitHub runner, 300 buildings across all 29 types plus 200 units: **p95 21.95 ms against 33 ms**. Local re-measure after the Phase 2 additions: **p95 19.5 ms**, 16 draw calls, sim 0.62 ms/frame |
| Draw calls inside ADR-0012's budget | **PASS, measured** | **28 building batches, exactly the ceiling** (`test/view/economy-draw-calls.test.ts`); whole frame inside its derived ceiling (ADR-0014) |
| Determinism still bit-identical | **PASS in CI** | 20 orders, 13 intent kinds, 1 400 ticks; every economy order provably moves the hash (P2-T19) |
| Both renderers draw everything | **PASS** | Conformance green against WebGL2, Canvas2D and the recording fake, including the new `status` badge |
| The economy is *legible* | **NOT MEASURED** | `docs/playtests/economy.md` is written and unrun (ADR-0011). This is the criterion the phase actually rests on |

**The legibility debt, counted from the code rather than from the ADRs.** ADR-0014 put the total at
"thirteen entity types share a silhouette". Counting `meshIdForType` over the real roster says
otherwise, and the difference is not a rounding error — it is a category the ADRs never named:

| | Types | Meshes | Sharing | Kind |
|---|---|---|---|---|
| Buildings | 29 | 14 | **21** | Deliberate families (ADR-0013) |
| Units | 18 | 6 | **14** | 4 freighters, deliberate (ADR-0014 §1) + a 10-strong `worker` family, **9 of them with no mesh at all** |
| Commodities → deposits | 23 | 2 | **23** | Deliberate (ADR-0014 §1) |

The nine-building `factory` family is the one ADR-0013 argued for; `fortress` (3), `powerplant` (3),
`works`, `port` and `civic` (2 each) are the rest of it, so the buildings figure is **21, not 9**.

**The finding: nine units render as Workers.** `ranger`, `breacher`, `dreadnought`, `mender`,
`wraith`, `aegis`, `colossus`, `leviathan` and `heliumbomb` have no mesh of their own, so
`meshIdForType` falls back to `worker`. That is not a silhouette family — nobody decided a
Dreadnought should look like a Worker — it is the fallback firing on a roster Phase 1 only built the
MVP of. It is *in scope for Phase 3* ("the full roster"), and it is recorded here because the gate is
where an unmeasured fallback should stop being invisible.

**It also puts a number on Phase 3's budget problem, now, rather than at the end of it.** ADR-0014's
derived ceiling is computed over today's roster, where those nine share the Worker's mesh. Giving
each a real silhouette costs **two batches apiece — one per owner — so 18 more** at mesh LOD (the
imposter is a single shared mesh, so distance costs nothing extra). The 300-building Phase 2 scene
measures 27–28. The derivation will move with the roster, which is what ADR-0014 built it to do;
what it will not do is find the budget. That argument belongs in Phase 3, with a measurement,
exactly as ADR-0013's did.

Every collision above is a bet that the build menu and the panels carry a distinction the field no
longer does, and every one is unmeasured until the playtest runs. E3 in `docs/playtests/economy.md`
is the check, and its failure trigger is an ADR with a measurement rather than a task, because the
budget that forced the families has not moved.

**Six status glyphs (ADR-0015) are a vocabulary a player must be taught.** Shape is colour-blind-safe
and it is not self-evident. E2 measures it; failing it does **not** mean adding colour.

**What Phase 2 found that Phase 1 could not.** Four bugs that were green before they were looked at,
all of the same shape — a test and an implementation agreeing with each other about something the
engine never said:

1. Cargo fullness divided by a hardcoded 10, the *Worker's* capacity. Every logistics unit read as a
   full hold from its first crate, and P2-T05's own test agreed with it (P2-T07).
2. `countLogistics` and `countRepairJobs` are both **mutators** that stamp tallies onto sim entities.
   Two panels nearly wrote sim state on a frame, two phases apart (P2-T13, P2-T15).
3. `rigSurvey`'s node-list argument: passing all nodes compiles, runs, and leaks every unscouted
   deposit on the map through a build ghost (P2-T16).
4. The determinism fixture hashed only physical fields, so `paused`, `logiPriority` and `recycling`
   could stop reaching the engine with the hash unmoved (P2-T19).


## Phase 3 — Combat and the opponent

Goal: fights that read clearly in 3D. Exit criteria: PRD §5, Phase 3 — *a 20-minute AI-vs-player
match runs at budget; every combat cue is legible in a blind readability test; no combat feedback
allocates per frame.*

> Phase 2's gate is closed on its automated criteria (ADR-0011). Its one open criterion — legibility
> — needs five playtesters, so Phase 3 proceeds rather than waiting. Note that Phase 3 *adds* to the
> same deferred question, and the two scripts should be run in one sitting.

**What the engine actually says, which is why this decomposition looks like it does.** Read off the
vendored source, not the PRD's prose — the same exercise that found the PRD wrong about the recipe
chain in Phase 2. Four findings shape everything below.

- **The engine emits no "a shot was fired" event.** Thirteen event types exist across the whole
  simulation and the only combat one is `entityKilled`. There is no tracer, no impact, no
  damage-dealt. Combat feedback therefore cannot be event-driven: the only per-tick evidence a shot
  happened is `unit.attackTimer` resetting to `def.cooldown` and `unit.autoTarget` naming the
  victim, both of which the bridge must *diff between ticks* to turn into an event. That is Q-12,
  and it is the phase's central architecture question.
- **The bridge currently throws every event away.** `WorldBridge.step` runs
  `this.state.events.length = 0` with a comment saying nothing consumes them in the MVP. Alerts
  (P3-T14) and death feedback (P3-T06) both start by *keeping* them, which is a snapshot-width
  decision of the same kind ADR-0012 §1 made for the economy.
- **"Turret tiers" is not a tier mechanic.** `building.tier` is unrelated. The static-defence ladder
  is four separate building types — `turret` (20 dmg / 130 range), `bastille` (32/115, needs a
  Foundry), `torpedobattery` (55/180, ammo-fed from `plasmatorp`, needs Torpedo Works) — plus
  `aegisbastion`, which **has no attack at all**: it is a `guardAura` granting −20% damage taken
  within 130. Three of those four share the `fortress` mesh (ADR-0013), so today a player cannot
  tell the thing that shoots hardest from the thing that does not shoot.
- **The roster gap is exactly the nine the PRD names.** `ranger`, `breacher`, `dreadnought`,
  `mender`, `wraith`, `aegis`, `colossus`, `leviathan`, `heliumbomb` have no mesh and fall back to
  `worker`. The PRD is *right* here, unlike Phase 2 — but its scope line also lists **veterancy
  chevrons, which Phase 1 already built** (P1-T15, `rankOf` + the chevron overlay). The PRD should
  be amended.

**Two scope notes that are decisions, not observations.**

- Eight engine commands exist that the bridge has never exposed: `issueEscort`,
  `issueHoldFormation`, `issueAssistBuild`, `issueScout`, `issueFerryFreighter`, `issueSetHomeBase`,
  `issueSetAILogistics`, `issueSetCollectPoint`. Formations and escorts are therefore *wiring*, not
  invention — `issueMove` already takes a `formation` argument.
- **Control groups exist nowhere** — not in the engine, not in the client. They must stay client
  state: `state.selection` is sim state — it lives on `State` and `applyIntent` reads it to decide
  who every order applies to — so putting a control group there is a local, per-player convenience
  deciding what the simulation does.
  **Corrected during P3-T17**: this bullet originally added "so it would change the determinism hash
  and break every recorded replay". It would not. `hashState` covers units, buildings, resources and
  the Phase 2 economy fields, and deliberately **not** the selection — hashing it would make every
  `select` trivially covered by P3-T17's per-order check, which is how that check found two selects
  in the Phase 2 fixture that no later order ever read. So nothing in the repo would catch a control
  group written into `state.selection`; the rule is asserted directly instead, in
  `test/bridge/formations.test.ts`.

| ID | Task | Status | Deps | Definition of done | Notes |
|---|---|---|---|---|---|
| P3-T01 | ADR: the draw-call budget for nine new unit meshes | DONE | — | An ADR with a **measurement**, deciding families vs. full silhouettes vs. a raised ceiling; the Phase 2 scene re-measured after | **ADR-0016: all nine get their own silhouettes. No families this time — and that is a different answer from the last two, reached by measuring instead of pattern-matching.** Measured on the Phase 3 combat scene (300 buildings across all 29 types + 200 units across all 18, T0): **37 batches today, 55 with nine own meshes, against ADR-0014's derived ceiling of 83** — which itself moves to 119 once the meshes exist. **The budget does not bind.** ADR-0013 and ADR-0014 were both forced by 28 building batches of 28 available; that premise is simply absent here, and applying their conclusion without it would be pattern-matching. Two further measurements: distance is free (past `tier.lodDistance` everything collapses to the single shared `imposter` mesh, so +18 is the worst case, every one of the nine simultaneously in mesh range), and P2-T18 already showed that at scale the **sim** consumes the budget, not the view. The legibility side is unusually sharp for these nine: `mender` has **no attack** and currently looks like a Worker in an army; `heliumbomb` has no attack either and detonates for up to 3 000 damage in a radius — the most expensive misread in the game, in both directions; `ranger` is the scout at 115 speed against a Worker's 60; and `breacher`/`colossus`/`leviathan` fight at 150–200 range while `aegis` fights at 26. **Trigger stated in advance**: if P3-T16's browser gate breaks the 33 ms T0 budget, ADR-0016 is superseded with that measurement, and the named fallback is families for the three siege units |
| P3-T02 | Meshes for the nine unmeshed units | DONE | P3-T01 | Every `UNITS` key resolves to a mesh that was actually built; no type silently falls back; the silhouette test covers the new ones | Nine silhouettes, shape following function: the Mender is the only unit **wider at the top than the bottom** (a dish, and nothing on it points forward — every armed unit has a barrel, a nose or a prow); the Helium Bomb is the only **sphere**, widest at its waist; the Ranger is the narrowest spire; the Colossus is the tallest and thinnest above its base; the Aegis is the widest and lowest; the Leviathan is the largest. **The fallback assertion is the one ADR-0016 named**, and it is mutation-proofed three ways: dropping a mesh, weakening the check to registered-only, and *laundering* the fallback as an explicit `UNIT_FAMILY[ranger] = "worker"` entry each turn the suite red. **Two measurement errors found and fixed along the way, both in code that was already green.** (1) The shared silhouette metric sampled width in a ±15% band around each height, so when *neither* mesh had a vertex there both read 0 and the metric scored that as perfect agreement — it had the Helium Bomb and the Skiff at 0.223 when an 8-tall sphere and a 2.4-tall dart are nothing alike. Width is edge-interpolated now (`test/view/silhouette.ts`, shared with the buildings test), and the corrected metric immediately surfaced two real collisions: freighter/aegis at 0.065 and mender/ranger at 0.185, both reshaped. (2) A first draft asserted mesh radius ordered like `UNITS[t].radius`; that is a **bounding** radius against a **collision** radius, so it called the Lancer bigger than the Bastion because the Lancer is mostly barrel — by deliberate design, per its own comment. Replaced with a claim that is true and matters more: **a mesh may not exceed the selection ring the engine draws for it** (`radius x 1.35`, scene.ts), which found that the Colony Ship already overflows its own ring 22.11 against 17.55 — named as a known deviation rather than silently reshaped in a Phase 3 commit |
| P3-T03 | Static-defence ladder reads as a ladder | PARKED | P3-T01 | `turret`, `bastille`, `torpedobattery` and `aegisbastion` are distinguishable; the Aegis Bastion does not read as a weapon | **The Aegis Bastion's half is closed, by its aura rather than by a mesh (P3-T04).** It cannot be closed by a mesh: the buildings cap is a hard, hand-written 28 sitting at **exactly 28**, so a fourth defence mesh needs an ADR arguing against the number that cap exists to defend. The aura settles it better than a mesh would have — the ring is not an arbitrary badge meaning "this one is different", it is the building's actual function at the engine's own radius, and it costs one overlay layer for the whole frame against the two batches a mesh would have taken. **Still open, and asserted as an admission so it cannot quietly stop being true**: `bastille` (32 damage / 115 range) and `torpedobattery` (55 / 180, ammo-fed) remain the same silhouette and differ only in reach. `test/view/aura-overlay.test.ts` pins the fortress family membership, so giving either its own mesh fails that test on purpose — closing this gap costs two batches against a cap at 28 and must not happen by accident **Reclassified by P7-T04.** The Aegis Bastion half closed in Phase 3 via its aura. The other half — `bastille` and `torpedobattery` sharing the `fortress` silhouette and differing only in reach — cannot be closed without an ADR arguing against the hand-written buildings cap of 28, which sits at exactly 28 and exists to be argued against rather than edited. **Two triggers, either sufficient**: an ADR that moves the cap with a measurement, or `combat.md`'s silhouette gate failing with real testers, which is the evidence that would justify spending the batches. Held as an admission test so it cannot quietly stop being true. |
| P3-T04 | The Aegis Bastion's guard aura is visible | DONE | P3-T03 | The `guardAura` radius (130) renders; a player can see their own damage-reduction coverage | The engine had already done the work and the bridge was throwing it away: **`collectAnvils` rebuilds `state.anvils` every tick** from live positions — `{id, owner, x, y, range, mult}` — folding the Aegis UNIT (range 96) and the Bastion (130) into one shape. So the bridge copies rather than derives; a view that hardcoded either range would have been wrong about the other, which is why both are asserted. A new `aura` overlay kind, drawn as a ground ring through the same `drawGroundEllipse` the selection ring uses, so it deforms over a slope instead of floating flat above one. **Dashed, not solid** — a permanent solid ring around a building reads as a selection that will not clear — and owner colour is the second cue rather than the only one (N-05). Three judgements worth naming: the **multiplier deliberately does not cross the bridge** (it is a number for a panel, and carrying it beside a radius on the same row is how one gets drawn as the other); **enemy auras are fog-gated on live vision, not memory**, because a ring visible through fog is a map hack with a nice name and one remembered where an Aegis used to be would tell a player their army is protected where it is not; and the **cull radius is widened by the aura's own radius**, so a projector just off-screen does not blink its ring away at exactly the screen edge a player is watching. **Verified by mutation**: never pushing the overlay, drawing the multiplier as the radius, and dropping the fog gate each fail the suite |
| P3-T05 | **Q-12**: how a shot crosses the bridge | DONE | — | Decided in an ADR: per-tick diff of `attackTimer`/`autoTarget`, an engine event that does not exist, or something else | **ADR-0017: a shot is a diff, not an event.** Four things measured on a 120-unit fight over 400 ticks before deciding. (1) `attackTimer` is written in exactly two ways — three sites decrement it, three reset it to `def.cooldown` on firing — so **a rise between ticks is a shot, exactly**, an invariant of the engine's own code rather than a heuristic. (2) A unit cannot fire twice in a tick **by a factor of seventeen** (cooldowns 0.85–2.6 s against 0.05 s), so a per-tick diff loses nothing. (3) Volume is small: p50 **1** shot/tick, p95 **6**, max **33**. (4) **The measurement that decided the design: 12.9% of shots have a target that no longer exists by extraction time**, because the engine applies damage and removes the corpse inside the same tick — so *the killing shot* is precisely the one a lookup-and-skip implementation drops, while looking perfect in any test without a death in it. The endpoint comes from `prevPos` instead, which still holds where the target died because `rememberPreviousPositions` runs at the END of extraction. An unresolvable shot is **counted, not dropped silently**. Shots are gated on the SHOOTER's visibility: a tracer from unexplored ground is a line pointing straight at whoever is standing in it. The `attackTimer` invariant is asserted **against the engine's source**, not against a comment |
| P3-T06 | Combat feedback: tracers, impacts, death | DONE | P3-T05 | Who is shooting whom is readable in a still frame; **zero allocation across 600 frames of a firefight**, asserted | A **pooled** effect buffer, because the sim reports a shot once at 20 Hz and the display runs at 60+: an effect has to outlive the tick that made it, which means view state the snapshot does not have — exactly the shape that invites an object per shot. `ingestTick` is the only method allowed to grow and runs on a sim step; `age` runs per frame and retires by **swap-remove**, O(n) with no allocation. Growth *preserves* contents, unlike the snapshot's tables which refill from scratch — dropping on resize would make tracers vanish precisely when a fight got big enough to need them. Two overlay kinds, because they are two geometries: `tracer` is a line, `blast` is a point with a radius; merging them would mean a stride with dead fields in half its rows. A blast is an **expanding ring with a cross through it** — motion is the cue, and the cross keeps a ring at distance from reading as a selection (N-05). **The zero-allocation criterion is asserted over 600 frames of a RUNNING firefight**, not a quiet scene: an empty pool never grows, so 600 idle frames would pass with any implementation. `src/view/effects.ts` is also in the layering scan's HOT_FILES. **Verified by mutation**: a `new Float32Array` inside `age()` and a `slice()` during compaction each fail. **The perf harness was silently not measuring this** — it never called `ingestTick`, so the gate would have reported combat feedback as free. Wired in, and the draw-call baseline re-recorded 16→17 / 18→19 for the one tracer layer (67 live tracers at T0, 246 at T2, one draw call each) |
| P3-T07 | The bridge stops discarding engine events | DONE | — | Events survive the tick and reach the view; the drain still happens exactly once, so nothing grows without bound | **The bug was an ordering one and had been there since Phase 1.** `WorldBridge.step` cleared `state.events` *before* calling `refresh()`, so the extractor saw an empty list every single tick — `entityKilled`, the one combat cue the engine hands over ready-made, was thrown away with nothing ever having a chance to read it. Extract first, then drain; the drain still happens exactly once per step, asserted. Deaths cross as their own preallocated table (position, owner, building-or-unit), **per tick and never a running log** — a twenty-minute match would otherwise accumulate every death it ever saw. Gated on *explored* rather than *visible*, unlike shots: a wreck the player walks up to later should still have flashed, but a death in ground they have never seen is not theirs to know about |
| P3-T08 | Wreckage renders as salvage, not as an ore seam | DONE | P3-T07 | A matured wreck is distinguishable from a natural deposit at camera distance | **ADR-0018, and it reverses ADR-0014's answer on a measurement.** Measured on the Phase 3 combat scene: the whole frame is **60 batches against a derived ceiling of 119 — 59 of headroom** — and deposits occupy exactly **2** of it (`node|2|0`, `volatile|2|0`). **A deposit mesh costs ONE batch, not two**: deposits are neutral, so one owner slot, and `pushNodes` batches at `LOD_MESH` unconditionally, so no imposter row either. That is half what a unit or building mesh costs, which is why ADR-0014's arithmetic did not transfer. ADR-0014's actual reason for refusing a third mesh was that metallic-vs-crystalline is a **build-menu** distinction. Salvage-vs-natural is a **field** distinction: it says a fight happened here, it is finite where a seam is permanent, and it changes where workers go. **Origin beats contents**: a wreck of metals and a wreck of ore are both wreckage. The player loses the commodity at a glance — a real cost, and the panel already carries it — and gains the fact that an army died there. An unrecognised origin falls back to natural, deliberately: a false "a fight happened here" is worse than a missed one. **Verified by mutation**: ignoring the origin, and collapsing craters into salvage, each fail |
| P3-T09 | Craters render, and read as bomb damage | DONE | P3-T07 | A `craterMatured` node is distinguishable from both a natural deposit and a wreck | Same plumbing as P3-T08 (`n.crater` beside `n.wreck`), its own mesh because a crater and a wreck are different events. **The shape claim was walked back mid-implementation and that is worth recording.** The first draft called it "a hole, not a pile" — and a hole is not drawable here: `Builder.prism` always caps its top, so every shape it makes is a solid, and a concave bowl needs a rim plus a separate floor, which measured **60 triangles against a 30 budget**. An inverted frustum was the second attempt and **the winding test rejected it outright** for enclosing negative volume. Both salvage and crater were cut to fit rather than having their budgets raised — the P2-T17 precedent (the volatile deposit went five-sided rather than moving a 30). So the cue is **proportion, not depth**: the widest and by some margin the flattest thing on the ground, where the rock, the volatile and the salvage all stand up. Weaker than excavation would have been, asserted as what it actually is, and flagged for the playtest rather than argued away in a comment |
| P3-T10 | Helium Bomb: fuse, blast radius, and the warning | DONE | P3-T02, P3-T07 | `BOMB_FUSE_DELAY` (4 s) is visible on a lit fuse; `BOMB_BLAST_RADIUS` is shown before it goes off | **The row's own note pointed at `bombFused`, and the event is the wrong source — the exact inverse of ADR-0017's answer for shots, which is why it is worth stating.** A shot leaves no trace: nothing in the state says a unit fired, only a timer that was reset, so a diff is the only honest signal. A fuse is the reverse — `fuseUntil` is a real field that persists for the whole four seconds, while `bombFused` fires on **one tick in eighty**. A warning built on the event is correct for 1.25% of the time it needs to be on screen, and a player looking elsewhere on that tick gets nothing. Worse, **disarming cuts a lit fuse and emits nothing at all**, so an event-driven warning counts down to a blast that is never coming. Both are asserted, the first by counting warned ticks on which no `bombFused` was in `state.events` (80 of 80). **Two radii cross, not one**, because the engine's own `bombDetonated` event carries two — and they are asserted against `bombDamageAt` rather than copied: at `core` the curve is still at `BOMB_MAX_DAMAGE`, past `blast` it is zero. One circle would promise a kill at the rim the falloff does not deliver. The countdown draws as a **sweeping arc**, not a pulse, so the time left survives a still frame; the arc and the ring share one helper so they cannot disagree about where the circle is. Fog is two rules, not one: **the player's own armed bomb always shows** (the blast is not owner-exempt, so the ring is mostly a picture of the player's own units standing in it — asserted), while **an enemy's shows only once its fuse is lit, and only on live vision** — an un-fused enemy ring hands over a device the player was never told was armed. `arm`/`disarm`/`detonate` are the only intents in the bridge that write a sim field rather than calling an `issue*`; there is no engine command, and the engine's own AI does the identical flip (`aiSuperweapon.js`). They run inside `applyIntent`, so they land at a tick boundary in recorded order like everything else — asserted, because a flip anywhere else desyncs a replay. `detonate` lights a fuse rather than blowing up on the spot: the player's own trigger must not have a property the enemy's proximity trigger lacks. **Ten mutations checked**: dropping the fog gate, leaking an un-fused enemy bomb, sourcing the warning from the event, forgetting to cut the fuse on disarm, detonating instantly, collapsing the two radii, dropping the owner check, drawing one ring, ignoring the fuse fraction, and drawing no arc — each fails the suite |
| P3-T11 | Formations: shapes, leader position, hold | DONE | — | `FORMATION_SHAPES` (grid, line, wedge, circle) and `LEADER_POSITIONS` (front, back, center) round-trip through the UI; slots match `formationSlots` | Wiring, as the heading above predicted: two intents, four engine exports, no new rules. **The test's first draft asserted the wrong mechanic and that is the finding worth keeping.** It claimed every unit gets a `move` order to its own slot; `dispatchFormation` does not do that for a *player* selection — the leader gets one `move` and every follower gets `follow-leader` carrying an offset, a squad that keeps station on the leader's LIVE position rather than six independent moves that drift apart the moment one is blocked. (AI and owner-less units take the other branch and do get an order each.) So the test now asserts the real thing: exactly one leader under way, everyone else keeping station, and **offsets distinct and not all zero** — the last two are what separate a formation from a blob, and a bridge that dropped the formation argument would still produce one leader and five followers. The shape list is asserted against the engine's rather than copied: a UI offering a shape the engine does not know is a button that is silently refused, and one missing a shape hides a feature that already exists. Unknown shapes and leader positions are **rejected with a message**, not ignored |
| P3-T12 | Escorts | DONE | P3-T11 | `issueEscort` reachable; an escorting unit's station matches `keepEscortStation`; the guarded ship is identifiable | The engine documents that **the caller must filter the target out of its own escort**, so the bridge does — a ship ordered to ring itself is a unit that has quietly stopped doing anything. Ring slots are fixed at issue time, which is what makes the formation deterministic, so the test asserts no two escorts share one. The persistent-follow property is pinned over 40 ticks *after arrival*: "the order is still there" is exactly what a player is relying on when they walk away from the screen, and an escort that cleared on arrival would look identical for the first second. A target that died between the click and the tick is a no-op, not a throw |
| P3-T13 | Control groups | DONE | — | Assign, recall and append; a group survives a unit dying; **the group is never written to `state.selection`'s owner, i.e. never sim state** | `src/input/control-groups.ts`, and the constraint really is the whole task: the class has no access to the simulation and holds nothing but id strings, reaching the sim only by being replayed through the ordinary `select` intent — the same path a mouse drag takes. Both halves are asserted: exercising every method leaves `state.selection` untouched, and a recall does reach the sim when replayed. **Pruning happens on read, not on death**, because nothing tells this class an entity died and nothing should; the pruned list is written back, so a group that lost half its army does not re-filter the corpses forever and its badge stops counting ghosts. `size` deliberately does not prune — a HUD badge must not have side effects |
| P3-T14 | The alert system | DONE | P3-T07 | Each engine event that a player must act on produces exactly one alert, positioned, and dismissible; no alert fires for an event the player cannot see | **Coalescing is the task; everything else is bookkeeping.** `detonateBomb` pushes one `entityKilled` per casualty, so a bomb in a ring of ten Skiffs puts **eleven events on one tick at eleven positions spread 140 units** — asserted, because without a rule that is eleven identical HUD rows and the next attack is buried under them. The rule is **same kind, within `ALERT_MERGE_RADIUS` of the anchor, within `ALERT_WINDOW_SECONDS` of the FIRST event**; the radius is 190 because `BOMB_BLAST_RADIUS` is — the widest circle in which the engine kills an arbitrary number of things at once — and the test pins it there, since `view/` may not import the engine to say so itself. The window **does not slide**: a sliding one gives a twenty-minute siege exactly one alert, raised at the start and never repeated. The anchor never moves either — an alert is a bookmark read now and clicked a second later, so a drifting centroid lands the camera somewhere other than the mark that prompted the click. **A dismissed alert keeps its slot as a tombstone** until the window closes; free it and the next casualty of the same fight opens a fresh alert 50 ms later, which is this row's "must not resurrect" clause failing without the code ever looking wrong. A tally flag separates event streams from state: `snap.deaths` is one row per kill and accumulates, `snap.bombs` is the *same device* on all 80 ticks of its fuse and must not, or a four-second fuse badges ×79 and reads as a swarm. **Fog is inherited, not re-implemented — and the obvious fog test proves nothing**, because an AI death is dropped by the owner filter before fog is ever consulted; so the test kills the **player's own** units in never-explored ground and extracts the identical tick twice through one extractor, differing by one byte of fog. That owner filter turned out to be **a second fog gate in its own right**: `extractDeaths` gates on *explored*, not *visible* (P3-T07, deliberately), so an AI death in ground scouted an hour ago **does** cross the bridge — asserted — and a row about it would be a live report from territory the player cannot currently see. Alert kinds stop at what crosses the bridge today, losses and a lit enemy fuse; nothing was invented. **27 mutations, 27 caught — but only after the fourteenth**, a `retire` off-by-one that never re-checks the entry swap-remove moved down, survived a first pass of thirteen and forced a compaction test that did not exist. That is why `src/view/alerts.ts` joins the layering scan's `HOT_FILES`, verified live by planting an allocation in `retire()`. **Wired, not left on the shelf** (see the reachability row below): `ingestTick` runs once per SIM step beside the effect pool — a second pass over the same snapshot would tally one casualty as two — and **Space** jumps to the newest live alert and dismisses it, which is PRD §4's "focus last alert" finally having a board to jump to, with **Home** keeping the base so a press with nothing raised is a fallback rather than a dead key |
| P3-T15 | The AI opponent, live under its own fog | DONE | — | The AI's decisions are asserted to use `state.fogAI` and never `state.fog`; a scripted match shows it reacting to what it has actually seen | **Proof, not feature — the engine already played fair, and `test/engine/ai-fog.test.ts` now says so in four ways that go red when it stops.** The source scan is the valuable one and the fiddly one: **three of the four `state.fog` mentions in `ai*.js` are header comments promising the AI does not do that**, so the scanner strips comments before matching, and the single real read — `aiWorkers.js`'s `assignIdlePlayerGather`, the player-side twin `colonyPolicy.js` calls — is exempted by file **plus function plus exact statement**, not by module. `state.fog` is an *alias* for `fogs.player`, so the cheat has four spellings and all four are matched; the scanner is itself run against synthetic cheating sources so it can never quietly stop matching. Behaviourally, a Ranger walks the map in 40 scripted hops and `sightEnemy` must agree with `isVisibleAt(state.fogs.ai, …)` at **every one** — an equivalence per tick, not a before/after pair, and the reveal lands on hop 29 at exactly 8 × the Lancer's ore value, so it is a complete sighting rather than one unit clipping a fog cell. `INTEL_FADE` is asserted at **three cadences spanning three orders of magnitude**, because the compounding-fade bug `aiIntel.js`'s own header records was green under every test that jumped the clock once. Asymmetry is asserted in **both directions**: a one-sided cell-count difference is satisfied by an AI whose vision is a superset of the player's, which is what cheating looks like from outside. **Eleven mutations, all behaved**, and two are the honest limit of the whole exercise — deleting the `isVisibleAt` gate, and recomputing the AI's fog from the player's units, leave **no textual trace a regex can find** and are caught only behaviourally, which is why claims 2–4 are not ceremony. **Mutation testing also found a flaw in the test itself**: a cheat inside a `function*` was blamed on the function above it, because the enclosing-function regex did not handle generators — fixed and regression-tested, since misattribution is exactly how a real hit lands on an exempted name and gets waved through. Smaller findings: the AI is genuinely *less* informed than the player at the opening (88 visible cells against 95 on Helix), and the two `state.fogAI` occurrences in AI modules are default parameter values only — inside a body either would be the mirror-image unfairness, the self-play controller planning off the AI's vision |
| P3-T16 | Perf: the Phase 3 combat scene and its gate | PARTIAL | P3-T02, P3-T06 | A 20-minute AI-vs-player match holds the T0 budget; combat feedback allocates nothing per frame | **ADR-0016's supersede trigger did NOT fire.** The new `P3` scene puts every unit type on the field at once — all fifteen unit meshes, both sides packed into weapons range so shots and deaths actually happen — and measures **p95 5.69 ms against a 33 ms budget, 43 draw calls**. The nine silhouettes ADR-0016 bought are affordable, measured on the gate that ADR named as the thing that would overturn it. **It also closed two gaps in the harness, both of which had been making the gate report better numbers than the game deserves.** (1) `populate` assigned owner with `i % 2` against a FOUR-type unit mix, so type and owner were perfectly correlated and every unit mesh was counted for one side only — the identical bias that understated ADR-0016's own figures until P3-T02 caught it. (2) The harness kept its own extractor and ran it *after* `bridge.step` had drained `state.events`, so no death ever reached it, the `blast` overlay never fired, and every tick paid for a second full extraction the game never pays. Baselines re-recorded: T0 17→22, T2 19→23, draw calls only. `Baseline.p95` may now be **null**, explicitly, for a scene not yet measured on a machine worth trusting — the previous behaviour made the ceiling NaN and deleted the check in silence. P3 is gated on the PRD's own budget and its draw-call count meanwhile. **Still PARTIAL**: the row asks for a *20-minute match*, and what is gated is a representative frame at the budgeted counts. A 20-minute soak is a different test — entity counts drift as units die, which is the exact non-determinism this harness was built to avoid — and it is on the deferred-verification table rather than quietly counted as done **P7-T04 left this one PARTIAL on purpose**, and it is the only one of the four that stays: the other three were reclassified because no work in this repo closes them (PARKED with a trigger) or because they need people (BLOCKED ON HUMANS). This one's open half — the 20-minute soak — is real, automatable engineering that nobody has done, and PRD §5 names it as Phase 3's own exit criterion. Calling it PARKED would be filing a task as an obstacle. It is P7-T06. |
| P3-T17 | Determinism fixture extended with combat orders | DONE | P3-T11, P3-T12, P3-T13 | The recorded replay includes escort, formation and attack orders and hashes identically; each provably moves the hash | Fixture goes to **2 200 ticks and 30 orders** — `attack`, `escort`, `holdFormation`, `moveInFormation`, plus `patrol` and `stop`, which it had never carried either — and the coverage check is now a **loop in `replay.test.ts`** that replays once per order with that order withheld, runs by default, and costs ~12 s. **Run first against the Phase 2 fixture it inherited, it found four orders P2-T19's hand check had not.** Two `select`s no later order read: `applyBuild` takes the *first* builder in the selection, so the select changed nothing (now names the **last** colonist, so it decides who builds and, through `firstLegalSpot`, where); and `train` is **id-addressed**, so no select in front of it can ever reach the hash — deleted outright, with the reason recorded. A `pause` that could not move the hash because **`updateProductionQueue` never reads `paused`** and the resume overwrote it. And the recycle/cancel pair, moved off the Barracks onto a working colonist: **a recycling *building* stays fully functional**, which is exactly why the pair cancelled out, while a recycling *unit* cannot act, so one second of it shifts the gather cycle for good. It then found **six of its own, five from one cause** — the escorted squad arrived at a coordinate the script named and stood on it, erasing every positional order behind it — and a sixth, a `patrol` issued inside the enemy base where each leg was interrupted by a target in range before it could start. **Every fix was to the scenario, not to the check**: `hold` now halts the Skiff mid-journey, `holdFormation` fires while the squad is still closing so its anchor is a live centroid, the escorted advance never reaches its destination, and the patrol runs in open ground with `stop` on a leg that is actually moving. All 30 orders now move the hash. **Mutation-tested** with a `stop` on an empty selection at tick 0: flagged, alone, by index and tick — and removing it re-recorded to the identical hash, which also proves the recorder reproducible. `armBomb`/`detonate` stay out and it is not an oversight: a Helium Bomb needs a Star Dock behind an AI Foundry *and* a Torpedo Works plus gas/antimatter/plasma torpedoes, this scenario ends with 140 ore and no gas industry, and a hand-placed one would not exist on replay. **It also corrected a claim this board had been repeating** — see the control-groups bullet above: `hashState` does not hash `state.selection`, and hashing it would make every `select` trivially covered, which is how the two decorative ones were found |
| P3-T18 | Phase 3 playtest script | DONE | P3-T06 … P3-T14 | Script exists; written and left UNRUN under ADR-0011 | **`docs/playtests/combat.md`, written and left UNRUN.** Twenty-five tasks across four prepared saves (a still line-up, a battlefield the tester did not fight on, a live fight, an armed bomb), each carrying a named claim, a pass and a fail, rather than a "does this look clear". **Six tasks aim straight at what the board already admits**: the `bastille`/`torpedobattery` collision is asked head on and then as a forced choice ("which of those two would you rather fight?"), the crater's proportion-not-depth cue is tested *beside* a rock and a volatile in one view rather than in isolation — with "is it a hole?" scored as an informative failure, because it names the shape that could not be drawn in 30 triangles — both corrected-metric near-collisions get a forced choice, and the Mender/Helium Bomb misread is asked in both directions at once. **That last is the only gate in this project set at 5 of 5**, on ADR-0016's own reasoning: a 50/50 question four testers pass is a coin landing well. Ordering is argued, not incidental — the ground runs **before** the fight and the bomb, because a tester who has watched a detonation knows what a crater is and the crater question can only be asked once; and the Colony Ship ring question is **last on purpose**, because asking it early turns the rest of the session into a bug hunt. Runs in one sitting with `economy.md`, economy first, and names the contamination that buys. **Its most valuable output arrived before any tester saw it**: writing the setup surfaced that `ControlGroups` was referenced nowhere outside its own module and that no key or click produced a formation, an escort or a bomb command — three rows reading DONE for work a player could not do (closed; see the reachability note under the exit gate). Two setup traps are written in rather than papered over: an enemy bomb cannot be armed on cue without a second player, solved with a save whose `fuseUntil` fires ~4 s after load; and salvage needs 45–150 sim-seconds to mature and a crater 60, so a save taken as a fight ends contains neither |



## Phase 3 exit gate — status

Checked against PRD §5 (Phase 3): *"a 20-minute AI-vs-player match runs at budget; every combat cue
is legible in a blind readability test; no combat feedback allocates per frame."*

| Criterion | Status | Evidence |
|---|---|---|
| A 20-minute match runs at budget | **PARTIAL, measured** | The `P3` scene — every unit type on the field, both sides packed into weapons range so shots and deaths happen — holds **p95 4.51 ms against 33 ms, 44 draw calls**. But the criterion says *20-minute match*, and what is gated is a representative frame at the budgeted counts. A soak is a different test: entity counts drift as units die, which is the non-determinism this harness exists to avoid. On the deferred table, not counted as done (P3-T16) |
| No combat feedback allocates per frame | **PASS** | Asserted over **600 frames of a running firefight**, not a quiet scene — an empty pool never grows, so 600 idle frames pass with any implementation. `src/view/effects.ts` and `src/view/alerts.ts` are both in the layering scan's `HOT_FILES`, each verified live by planting an allocation |
| Every combat cue is legible | **NOT MEASURED** | `docs/playtests/combat.md` is written and unrun (ADR-0011), to be run in one sitting with `economy.md`. This is the criterion the phase actually rests on, and two of its questions are aimed at gaps this board already admits |
| ADR-0016's supersede trigger | **DID NOT FIRE, measured** | That ADR named P3-T16's browser gate as the thing that would overturn it. The nine silhouettes measure inside budget on the scene it named. The fallback (families for the three siege units) is not needed |
| Determinism still bit-identical | **PASS** | 2 200 ticks, 30 orders, and **every one provably moves the hash** — a per-order withholding loop that runs by default and found ten uncovered orders, four of them inherited from Phase 2 (P3-T17) |
| The AI plays under its own fog | **PASS** | Source scan plus three behavioural claims, eleven mutations (P3-T15). The two most plausible real cheats leave no textual trace, which is why the behavioural half is not ceremony |
| Both renderers draw everything | **PASS** | Conformance green against WebGL2, Canvas2D and the recording fake, including the new `bomb` overlay |
| Phase 3's orders are reachable by a player | **PASS, and it was not** | Found by writing the playtest script; see below. Closed for Phase 3, with a test that fails when any Phase 3 intent has no gesture producing it |

**515 unit tests, 1 skipped.** Two rows stay open on purpose and neither is quietly counted:
**P3-T03** (the Bastille and the Torpedo Battery still share the `fortress` silhouette and differ
only in reach — closing it costs two batches against a buildings cap sitting at exactly 28, so it
needs an ADR with a measurement, not a task) and **P3-T16**'s soak.

### An engine defect found in Phase 4, pinned rather than fixed — **and since fixed upstream**

> **Closed.** Reported as upstream issue #92, fixed in `alma92350/SpaceExploration-RTS` PR #95, and
> arrived here through `npm run sync:engine` (`93f607ae46fb` → `50ceb88d36f2`). The pinned test went
> red on the sync exactly as it was written to, and was **inverted** into a regression guard rather
> than deleted: `test/bridge/galaxy-save.test.ts` now asserts the price book is **unmoved** across a
> save on a world carrying real wreck nodes, with the control world kept. The section below is the
> record of the defect, not a live one — read it in the past tense. Two other rows cite it and both
> are stale in the same way; P5-T09's cell has been rewritten and says why a panel warning about it
> would now be teaching players to distrust a correct save.

**A world that has been fought over does not round-trip its market prices** (found by P4-T09). Not
ours to fix — ADR-0003 says the vendored engine is not forked — so it is asserted, with a control
world proving the claim is not the weaker "market prices never round-trip", and written so that an
upstream fix turns the section red and tells the reader to delete it.

The chain, each link verified against the source:

1. `galaxy.js:262` (`buildPlanetState`) computes a world's price book **once**, at creation.
2. `market.js:93` (`createMarket`) derives every raw commodity's `base` from
   `share = total[com] / sum` over **every map node's `max`**.
3. `wreckage.js:266` and `bomb.js:224` **push new nodes onto that set** for the rest of the game.
4. `persist.js:855` saves only `market.pressure` and `market.glut` — **never `base`** — and
   `persist.js:938` re-runs `createMarket(state)` on load, *after* `rehydratePlanet` has re-added
   those wreck and crater nodes.

So the loader derives the price book from a **larger node set** than the live book was built from.
`persist.js:938`'s own comment names the assumption that caused it — *"base recomputed from the
(regenerated) nodes"* — which is true of seed nodes and false of the wreck and crater nodes the line
above it restored. Reproduced through nothing but engine combat: twelve Lancers on Helix, 150
sim-seconds, four wreck nodes, **radioactives base 29 → 31 on load (~7%)**. It is not confined to
the wreckage's own commodity, because `sum` moves and every raw commodity's share moves with it, and
the drift **grows with accumulated wreckage**. A player saves after a battle, reloads, and every
price on that world is quoted off a different book. **The wreck nodes themselves round-trip
perfectly**, which is exactly why this is easy to miss.

### The reachability gap — a finding that outlives Phase 3

**Writing the playtest script found that Phase 3's orders were implemented, tested, and unreachable.**
`ControlGroups` was referenced nowhere outside its own module; no key or click produced a formation,
an escort, or a bomb command. Three rows read DONE for work a player could not do, and the script
that would have exercised them could not be run.

The wider version is not a Phase 3 slip. It is what this board has been closing rows *against*:

- **`src/input/intents.ts` emitted only Phase 1's gestures.** Thirteen intent kinds, all of them the
  MVP's. Every Phase 2 economy intent and every Phase 3 combat intent existed at the bridge with
  nothing producing it.
- **Six of the seven UI panels are imported by nothing.** `doctrine`, `market`, `operations`,
  `repair`, `research` and `rig` are reachable from no code path; only `building-panel` is, through
  `hud.ts`, and it is a *model* — it renders state and emits no command.

**This was a decision, not an accident**, and the decision is recorded: Q-10 and ADR-0012 §4 make a
panel a pure `(state | snapshot) → model` function tested without a DOM, and the Phase 2 exit gate
passes on exactly that — "eight panel models, each tested without a DOM". So the bar every row has
been meeting is the model layer, honestly and deliberately. What nobody wrote down is the
consequence: **"DONE" on this board has meant "the logic exists and is proven", not "a player can do
it"**, and the two deferred playtest scripts are the first things that need the second meaning.

Phase 3's half is closed — `f`/`t`/`p`/`o` and the digit row, Space for the alert board, wired in
`game.ts`, with a test that fails when any Phase 3 intent has no gesture producing it.

**Phase 2's half is closed too, as P4-T01**, and the answer was *wire them* rather than write the
model-layer bar into the definition of done. Both phases now carry an enumeration test that fails
**by name** when an intent has no control, which is the part that stops this recurring — the reason
it went unnoticed for two whole phases is simply that nothing in the suite was asking the question.

**And then it recurred anyway, four rows running.** Every Phase 4 row that built a screen built one
nothing opens: the starmap, the approach view, the colony/lane/policy panels, the jump panel. The
enumeration test only covers intents that exist, and a screen with no intent behind it is invisible
to it. So the lesson from P4-T01 was recorded in the wrong place — in a closed row rather than in
the definition of done — and each new row rebuilt the gap it had just closed. **P4-T13 installs the
rule instead of fixing the instance**: a row is not DONE until a gesture or a control produces it.

**P4-T14 is the same failure one level deeper.** Reachability assumed the *content* was reachable;
it is not. The build menu is a hand-written five of twenty-nine, so most of what Phases 2 and 3
built cannot be placed, trained, or seen in a running game at all.

## Phase 4 — The galaxy

Goal: Odyssey becomes Odyssey — more than one world. Exit criteria: PRD §5, Phase 4 — *a player can
settle a second world and run both; the galaxy save round-trips; background simulation holds its
frame budget while the active world renders.*

*Amended from the vendored engine, as the Phase 2 and Phase 3 headers were, because the amendment
changes what this phase IS:*

- **Almost the entire scope already exists in `engine/galaxy.js`.** Jumps (`canJump`, `canJumpTo`,
  `jumpCost`, `jumpCapital`), the Spaceport staging ring (`SPACEPORT_CAPACITY` by tier,
  `stagedRiders`, `jumpManifest`, `upgradeSpaceport`), the landing picker's own grid
  (`LANDING_PICK_GRID`, `snapLandingPoint`), colonies and income (`sweepColonies`,
  `COLONY_INCOME_PER_BUILDING`, `COLONY_INCOME_CAP`, `PACIFIED_INCOME`), freight lanes
  (`createLane`, `assignShipToLane`, `runLanes`, `LANE_PERIOD`), cargo (`CARGO_GOODS`,
  `loadFreighter`, `cargoManifest`), standing orders (`colonyPolicy.js`), and the background
  scheduler (`stepGalaxy`, `BG_STEP`). **Phase 4 is wiring and presentation**, the same finding
  Phase 3 made about formations and escorts — with two genuine exceptions below.
- **The background simulation already runs, and has since Phase 1.** `WorldBridge.stepWorld` calls
  `stepGalaxy`, not `tick`, deliberately (P1-T02's comment says so). So "background worlds" is not a
  feature to build; it is a **budget to measure**. `BG_STEP` spreads N worlds round-robin across 4
  frames, so the exit criterion is a perf row, not a systems row.
- **The two things that are actually new are both 3D**: the starmap (Q-03, **answered in ADR-0019**)
  and the landing picker rebuilt as an approach view. Everything else has an engine function behind
  it.
- **The reachability gap is Phase 4's, and it is first.** Phase 4's own scope is UI-first — a
  starmap, a picker, a lane panel, standing orders — so it cannot be built on top of an unresolved
  question about whether panels are reachable at all. P4-T01 settles it.

| ID | Task | Status | Deps | Definition of done | Notes |
|---|---|---|---|---|---|
| P4-T01 | Settle the reachability question, and pay down Phase 2's half | DONE | — | Every Phase 2 economy intent has a gesture or a control that produces it; the panel modules are imported by something; a test fails when an intent has no way in | The decision the Phase 3 gate deferred here. Six of seven panel modules are imported by nothing, so **"a player can run the full 2D economy in 3D with no panel missing" is not true on the plain reading** even though the Phase 2 gate passes it on eight panel *models*. Phase 3's half is already closed and `test/input/phase3-input.test.ts` is the pattern to extend, not to copy |
| P4-T02 | **Q-03**: starmap as a true 3D scene or a 2.5D diagram | DONE | — | Decided in an ADR, with a measurement rather than a preference | **ADR-0019: a 2.5D diagram on a plate.** This row framed it as a **draw-call** question and **that framing was wrong** — true 3D and the diagram both measure **8 draw calls against a derived ceiling of 119**, so the arithmetic had no opinion, and saying so beat manufacturing a difference. What decided it is a fact about the engine: **the galaxy has exactly one coordinate.** `PLANETS` carries 20 `x` values and **no `y` or `z`**, and the only two engine functions that read a world position — `jumpCost` and `checkExpansion` — both read `Math.abs(Δx)`. Lanes carry no geometry at all; `runLanes` moves cargo on a fixed `LANE_PERIOD` regardless of distance, so adjacency is purely topological. **A 3D layout therefore means inventing 22 numbers nothing in the engine can validate**, and `perf/starmap-probe.mjs` prices what they cost: for every seat × every pair of destinations, at all 8 snapped yaws, a galactic shell ranks jump destinations **backwards 37.6–45.9% of the time**. The most faithful 3D layout available reaches 5.4% — **no better than the plate's 5.2%** — while adding 3 marker collisions and requiring the free-orbit camera ADR-0010 refused under Q-01: a reopened decision for nothing that measures. **ADR-0005 §4 is why the collisions are fatal**: both renderers draw overlays to a flat canvas over the scene with **no depth test**, and a starmap is almost purely overlay — `galaxyStatus` reports nine channels per world, none of them geometry. Canvas2D was never the throughput problem (720 triangles against the 13 086 it already sorts at T0); it is the depth problem, and so is WebGL. **Trigger stated in advance**: if a world ever gains a second coordinate, or P4-T12's playtest finds markers cannot be attributed to worlds, ADR-0019 is superseded with the probe re-run. "It looks flat" is explicitly **not** a trigger. The one-coordinate premise is load-bearing and `sync-engine.mjs` could land a second coordinate silently, so **P4-T03 owes a test** that asserts one coordinate and two `Math.abs(Δx)` read-sites |
| P4-T03 | The starmap: worlds, claims, stances, alerts | DONE | P4-T02 | Every world in `galaxy.worlds` is placed, owned and legible; a stance change is visible without opening a panel | **ADR-0019 decided the design; this built it and pinned the ADR's own premise before it could rot.** The plate is `perf/starmap-probe.mjs`'s measured layout unchanged, and the test **re-measures it through the composed frame** rather than a model of it: **5.219% discordant against `jumpCost`, 0 marker collisions**, with the bare-line alternative measured *in the same test* at **0.0% and 6 collisions** — so the stagger's price is asserted, not quoted. **The fidelity band is pinned in BOTH directions**, because 0.0% passes any ceiling and would mean the test had quietly started measuring the flat plate. `galaxyStatus`' sixteen channels are copied and compared field-for-field against a live call; the three things that are *not* copies are named where they happen, including `x`, which re-implements the module-private `planetX` and is kept honest by pinning the engine's accessor **by its exact text**. Two things the engine does not answer, both derived and both stated: which owner colour a status flies (ordered — **pacified outranks a claim**, because a flag over a world whose capital you razed is a flag over a graveyard), and what counts as an alert. **The stance is banded nowhere**: `stanceLabel` and `PEACE_THRESHOLD` stop at the engine, so the bar is monotone in the raw −1..1 number — a threshold invented above the bridge would be a second answer to a question the simulation already answers. **The premise test is ADR-0019's stated obligation and it fires on all three shapes of trigger**: a `y` in `data.js`, a second coordinate under a name no `y`/`z` denylist catches, and a `planetY` accessor with a `Math.hypot` read-site. Verified independently by adding a `ring` coordinate and watching it go red. 22 mutations, all red; the six against vendored files restored byte-exactly with `check:vendor` re-run |
| P4-T04 | Jumps and the Spaceport staging ring | DONE | P4-T03 | `canJump`/`canJumpTo`/`jumpCost` decide, never a re-derivation; the manifest a player sees is `jumpManifest`'s; tier capacity is visible before committing | `jumpCost` is asked and `JUMP_COST` never shown — proved with **two ports of different tiers**, because at Tier 1 the constant is only 13 credits wrong on this hop and at Tier 3 it is 116, so a single-tier assertion passes against a hard-coded 400. `canJumpTo` is asserted on **both branches in two galaxies**, including the one that matters: an army hopped to a portless world, where the only destination still open is the world its Command Center stands on — flagged as the control switch it is, because a fall-back carries nobody. **The manifest is reported whole, riders AND overflow**, because the fill is skip-not-break: the test parks a colony ship *nearer* the pad than the last colonist to board and asserts the ship is what stays, which a "last N" derivation gets backwards. A lane-booked freighter is in neither list. **16 mutations, two of which came back GREEN and were fixed**: pinning the row's capacity to `SPACEPORT_CAPACITY[1]` passed everything, because every assertion happened to read the manifest's copy or a Tier-1 pad; and dropping the landing point passed, because a world's default anchor is still closer to the snap than to the raw click. **A real defect fell out**: `galaxy.d.ts` declared `jumpManifest` returning `{riders, supply, capacity, left}` where the engine returns `{riders, capacity, used, stagedSupply, staged, leftBehind}` — it type-checked and was `undefined` at runtime, the exact fiction ADR-0003's façade exists to prevent. Corrected, and a test now pins the runtime keys, because a hand-written declaration is an assertion about JavaScript that nothing else checks. Recorded rather than fixed: a still-constructing pad is refused with "Not enough ore to extend the pad" |
| P4-T05 | The landing picker as a 3D approach view | DONE | P4-T03 | A landing site is chosen in 3D; the chosen point is `snapLandingPoint`'s, not the raw click | **The snap crosses the ADR-0008 boundary as the engine's FUNCTION, not as its number.** `ApproachBrief.snap` is `snapLandingPoint` itself, bound in `ui/landing-panel.ts` — a panel model may import the engine, `view/` may not — so `src/view/landing.ts` contains no grid, no margin and no rounding, and the marked point is the engine's *by construction* rather than by agreement. One step past `BombTable.fuseDelay`, deliberately: there the view divides by the constant and the arithmetic is its own; here it belongs to the engine, and two ways of copying it break — reimplementing round-then-clamp in the other order agrees everywhere *except* within a grid step of the edge, and **"the nearest reachable site" is not the snap** (x=90 snaps to 160 while 100 is reachable: 60 units wrong, and it looks right). `landingZone` is not exported and **does not necessarily use your point**: with a Spaceport standing it lands at the pad with the highest `lastLanding`, ties to the lowest id, and discards `landingPoint` — so the picker marks the winning pad, draws a dead ring on the discarded click with a line to where the colony really goes, and sends **no landing field at all**, because shipping a number the engine throws away is a promise the recording keeps and the game does not. **Writing it surfaced that `snapLandingPoint` is NOT idempotent** — 161 of 1601 x-values on a 1600-wide map, the whole clamp band; a click at x=0 displays at 100 and re-snaps to 160. So the intent forwards the RAW pointer point and snaps once for display; forwarding the picker's own answer would put the colony 60 units from the ring it drew. 18 mutations, all red, two of which caught assertions that were vacuous first — the marker-height check was two zeroes agreeing until it moved to a world with relief |
| P4-T06 | Colonies, passive income and credits | DONE | P4-T04 | `sweepColonies`' income reaches the HUD; the cap and the per-building rate are visible, not implied | The model states the rate (0.3/s), the cap (6), the per-colony ceiling and the occupation dividend, and every row says **what one more building is worth** — the full rate below the cap, exactly zero at it, shown *before* the ore is spent rather than after the counter stops moving. Counting non-turret buildings is the one rule the panel repeats (`incomeBuildingCount` is module-private), so it is pinned against the credits `sweepColonies` actually banks — **per world through a single-planet probe galaxy** as well as in aggregate, so a model that piled every world's income onto one row cannot hide inside a matching sum. Two predicates that agree in every situation the game produces are deliberately not interchangeable: `background` versus "not the seat" — `addPlanet` builds a world that is neither. **The finding underneath is that none of this income exists yet**: `stepGalaxy` does not call `sweepColonies` and neither does `WorldBridge.step`, so the colonies produce a rate nobody banks — stated as the first test rather than left to be discovered. A second engine defect reported, not fixed: **`galaxyStatus` reports `income: 0` for a pacified colony that is in fact earning** (measured at 60 credits/min against a reported 0), because `else if (pacified)` short-circuits before the income branch — which is why the panel does not forward that field, and why a starmap built on it would show nothing for the best-paying world |
| P4-T07 | Freight lanes | DONE | P4-T06 | A lane is created, assigned, and runs; `runLanes` is the mover; `LANE_PERIOD` is visible so a player can tell a slow lane from a broken one | The panel leads with the clock, because a lane is **periodic** and ~398 of every 400 ticks show nothing moving — which reads as broken. The central assertion is not that a countdown exists but that it is **true**: two full periods stepped one tick at a time, asserting cargo moves on exactly the ticks the countdown predicted and on none of the others. Capacity comes from `freightCapacity` and the manifest from `cargoManifest` — the same call `runLanes` makes, so the most-valuable-first load order that decides what gets left behind is visible — and one test deliberately starves the source so a "capacity" that was really a stock level cannot pass. `standing` separates a crew member the next run will use from one `runLanes` is about to **silently drop**, which is the entire difference between a slow lane and a broken one. The jump cost is first-class: `candidates` **is** `stagedRiders`, so the ring count and the offer list cannot disagree, and the held-back figure is measured in **supply, not hulls**. **Four mutations came back green and grew the scenario**: the candidate filter needed a worker in the ring, and supply-versus-head-count needed a Heavy Hauler — until then a panel counting hulls where it promises supply read identically to one summing `supplyCost` |
| P4-T08 | Colony standing orders | DONE | P4-T06 | `getColonyPolicy`/`setColonyPolicy` round-trip; `sanitizePolicy` is the validator, never a copy | `getColonyPolicy`/`setColonyPolicy` round-trip through the `colonyPolicy` intent with **every field moved off its default**, because a policy already at the off default round-trips through a setter that drops its argument; an unset world is separately asserted to read back as `sanitizePolicy(null)` rather than `undefined`. `MAX_WORKER_TARGET` is a clamp the UI must **show**, so `policyPreview` asks `sanitizePolicy` — the engine's validator, never a copy — what a request becomes *before* the intent is sent, and names each difference: 99 comes back as 20 with the ceiling quoted, alongside rounding, negatives, non-numbers and unknown commodities, each classified against `COM`, the same table the validator checks. The bridge deliberately does not pre-clamp, so there is exactly one opinion about the ceiling. **The other half of the row is a policy stored perfectly that does nothing**: `runColonyPolicies` acts on background worlds only, so an order set on the world under your feet is inert; auto-sell with no floor sells nothing; a worker target with no finished Command Center can never queue one — each asserted by running the real scan and watching the stockpile not move, then paired with the same order on a real colony that does |
| P4-T09 | The galaxy save round-trips | DONE | P4-T06 | A galaxy with two settled worlds serialises and deserialises to an identical hash, including lanes, policies and background world state | **A fresh galaxy round-trips even through a serializer that drops everything**, because `createGalaxy` and `deserializeGalaxy` both regenerate every world from the same seed — so the task is really about building a galaxy expensive enough to be worth saving, and the test asserts that divergence **per world** before it asserts anything else. The scenario settles a second world through the engine's own `jumpCapital` (387 credits of fuel), runs three background colonies 65 sim-seconds off their opening, crews a Freight Lane that has actually delivered, and sets a standing order that has actually sold into a market. The digest is deliberately **not** `replay.ts`'s `hashState` — P4-T11 is expected to widen that, and a save proof must not go red because a determinism fixture changed its digest. **The roster trap is real and exactly one test catches it**: deleting the `ODYSSEY_WORLDS` append in `deserializeGalaxy` leaves every hash claim green while silently freezing every world the save's roster predates, which is why the strongest assertion steps **both** galaxies 400 further ticks rather than comparing them at rest — they mint identical entity ids as well as identical state. Eleven mutations against `persist.js`, restored byte-exactly; one came back **green and correctly so** (the load-time `laneId` back-stamp is a redundant second carrier), and one crashed the first digest instead of asserting, which is why node amounts now hash through a non-finite-tolerant formatter. **And a real engine defect fell out — see below** |
| P4-T10 | Perf: the background simulation holds its budget | DONE | P4-T03 | A galaxy with the full roster settled holds the T0 budget while the active world renders; the background cost is measured per world, not in aggregate | Measured, not built. `P4` is T0's spec with `settledRoster` added and **nothing else**, so the two rows of `npm run perf` read against each other and the whole difference is the ten settled worlds. Ten worlds at 281 entities each cost **2.3 ms per background world-step**; the round-robin's period is 4 galaxy ticks, so **0.19 ms per world per frame amortised, 1.9 ms for all ten**, and the scene finishes at a **10–11 ms frame p95 against 33 ms** with the active world rendering. **The number that matters is none of those**: it is the **~7 ms lump** the three worlds of the busiest phase land on the one frame carrying their galaxy tick — exactly what a per-frame average hides. The period is **measured off each world's own clock rather than imported from `BG_STEP`**, because a harness that hard-codes 4 keeps reporting a tidy four-phase cycle after upstream stops having one; proved by removing the round-robin and watching the measured period go to 1. **The finding underneath: every perf run this repo has ever published already had three background worlds ticking inside it and called that "the background simulation running".** An `unsettled` world is **one unit and zero buildings** — so with `populate` removed the whole background cost reads **0.00 ms/frame and the gate still prints "within budget"**, and with the bridge stepping only the seat it reads the same. Three premises are therefore asserted, not assumed. P4's draw calls are **22, identical to T0's**, and that is the assertion rather than an accident: the background galaxy is simulated and never rendered, so a settled roster that moved this number would mean a colony had leaked into the seat's frame. **Also closed a gap found while recording it**: `P2` was the only gated scene with **no `baseline.json` entry at all**, and `judge` skips every check it has no baseline for — so the scene that validates ADR-0013's six families and the 28-batch cap had its draw calls ungated, and a family splitting back into per-type meshes would have passed in silence. Now gated at 35, verified by lowering it to 34 and watching it fail. Both p95s stay `null`: P2 measured **21.8 ms and 34.3 ms on the same commit** depending on machine load — a 57% spread straddling the budget in both directions |
| P4-T11 | Determinism fixture crosses a jump | DONE | P4-T04, P4-T09 | The recorded replay includes a jump and hashes identically; the jump provably moves the hash | **The hash had to widen, and the number is six.** `hashState` hashed the seat — and after a jump the seat is a different world, so under the old digest the per-order coverage loop leaves **6 of 35 orders inert**, every one an order whose only mark is on the world the expedition leaves. `hashGalaxy` now hashes **every world in `galaxy.planets` plus `activeId`**, which also closes a hole `world.ts`'s own header advertises: `step` calls `stepGalaxy` *specifically* so background worlds keep simulating, and nothing in the suite could see if they froze. **Credits and `discovered` deliberately stay out** — hashing either hands `trade` and `jump` the free pass P3-T17 refused `state.selection`. `activeId` earns its line as *state* rather than coverage: removing it leaves all 35 orders covered, but two galaxies with equal worlds and different seats diverge on the next tick. The fixture goes to **7 400 ticks and 35 orders**, and 5 200 of those ticks are ore rather than choice — a jump needs a Spaceport, which needs a Foundry, which with the 342-credit fuel is 545 ore from a world that pays 1.7 a second. **The jump is provable from both ends**: the destination is a *dormant* world, so withholding the jump leaves it absent from the galaxy entirely and the colonists still standing on Helix Belt. Two Phase-2 beats had to move and it was not tidying — a Barracks recycle completes in 200 ticks, so left where it was it razed the building, taking `paused` and the Foundry's prerequisite with it. Cost: the coverage loop goes ~12 s → ~110 s; the lever is the fixture's economy, not the check |
| P4-T13 | Wire Phase 4's screens to something a player can press | DONE | P4-T03 … P4-T08 | The starmap, the approach view, the colony/lane/policy panels and the jump panel are each reachable; the enumeration test covers Phase 4's intents | **The reachability gap recurred, four rows running, and that is a defect in the DoD rather than in the work.** P4-T01 closed it for Phase 2's economy; every Phase 4 row then rebuilt it, because "a control reaches it" was not in any row's definition of done. Four complete, tested, mutation-proofed modules currently reach nothing: `starmap.ts`, `landing.ts`, `colony-panel.ts`/`lane-panel.ts`, `jump-panel.ts`. **Also needed here and found by P4-T06**: `WorldBridge.step` never calls `sweepColonies`, so colony income is a rate nobody banks — the panel is right and the money never arrives. And `WorldBridge` needs a `get galaxy()` accessor; `replay.ts` currently reaches the private field through a guarded cast. **The rule this row exists to install**: a row is not DONE until a gesture or a control produces it, asserted by the enumeration test P4-T01 established |
| P4-T14 | The build menu is five buildings of twenty-nine | DONE | — | Every building the engine defines and the player can afford is offerable; `prereqsMet` does the gating, not a hand-written list | **Found by asking what a player can actually build today: five buildings and five units.** `MVP_BUILDINGS` is a hardcoded Phase 1 subset — Command Center, Barracks, Habitat, Refinery, Turret — and Phase 2 shipped meshes, panels and economy logic for all 29 types without ever widening it. The consequence compounds through the tech tree: **Foundry, Arsenal, Spaceport and Star Dock are all unbuildable**, so of 18 unit types only Worker, Ranger, Colony Ship, Skiff and Bastion can be trained. **Of ADR-0016's nine new silhouettes — measured, mutation-tested, perf-gated — exactly one (the Ranger) is reachable in play**, and the Helium Bomb's two-ring blast warning sits behind a Star Dock four buildings deep in a tree with no entrance. It also quietly undermines the deferred playtests: `combat.md` asks testers to tell a Mender from a Ranger, which no tester could build. The fix is to stop hand-writing the list — `prereqsMet` and `canAfford` already exist and the HUD already calls the latter, so a hardcoded subset is doing work the engine does better |
| P4-T12 | Phase 4 playtest script | DONE | P4-T03 … P4-T08 | Script exists; written and left UNRUN under ADR-0011 | Same shape as `combat.md`. The starmap is the first screen in this project that is not the battlefield, so "where am I" is a real question to ask |

## Phase 4 exit gate — status

Checked against PRD §5 (Phase 4): *"a player can settle a second world and run both; the galaxy save
round-trips; background simulation holds its frame budget while the active world renders."*

| Criterion | Status | Evidence |
|---|---|---|
| Settle a second world and run both | **PASS** | `jumpCapital` reachable end-to-end: `Y` opens the plate, a world opens the approach view, the confirm jumps. The determinism fixture crosses a jump and settles Verdani (P4-T04, T05, T13) |
| The galaxy save round-trips | **PASS** | 15 tests over a galaxy that has *diverged* — a second world settled, three background colonies 65 s off their opening, a lane that has delivered, a policy that has sold. Eleven mutations against `persist.js`, restored byte-exactly (P4-T09) |
| Background sim holds its budget | **PASS, measured** | Ten settled worlds at 281 entities each: **2.3 ms per background world-step, 1.9 ms/frame amortised, ~7 ms on the busiest frame, frame p95 10–11 ms against 33 ms** (P4-T10) |
| Determinism still bit-identical | **PASS** | 7 400 ticks, 35 orders, every one provably moving the hash — after the digest had to widen from the seat to the whole galaxy (P4-T11) |
| Q-03 answered | **PASS** | ADR-0019, decided on a measurement, with its premise pinned by a test that fires on a second coordinate under any name (P4-T02, T03) |
| Every screen is reachable | **PASS, and it was not** | The rule is installed, not the instance fixed: a runtime import-graph scan plus a real `Game` in a real DOM (P4-T13) |
| The galaxy is *legible* | **NOT MEASURED** | `docs/playtests/galaxy.md` written and unrun. It owns ADR-0019's supersede trigger |

**760 unit tests, 15 browser tests.** Two rows exist because the phase found its own gaps rather than
because the PRD asked: **P4-T13** (reachability, which recurred four rows running) and **P4-T14**
(the build menu was five of twenty-nine, so one of ADR-0016's nine silhouettes was reachable in a
real game).

**Engine defects found and pinned rather than forked (ADR-0003):** the post-battle market price-book
drift (P4-T09), `galaxyStatus` reporting `income: 0` for an earning pacified colony (P4-T06), and
`snapLandingPoint` not being idempotent across its clamp band (P4-T05). **And one live defect in our
own code, fixed**: HUD buttons captured their command at node creation, so Pause/Resume had been
sending the same payload since P2-T09 — found only when someone finally pressed the buttons.

## Phase 5 — The long game

Goal: everything that makes an Odyssey a campaign. Exit criteria: PRD §5, Phase 5 — *feature parity
with the 2D Odyssey; the parity checklist in `docs/planning/PARITY.md` is fully ticked.*

*Amended from the vendored engine and from the repo itself, as Phases 2, 3 and 4 were:*

- **`docs/planning/PARITY.md` does not exist.** Phase 5's exit criterion is "the parity checklist is
  fully ticked" and there is no checklist — so **the criterion is currently unmeasurable, and the
  document is not a deliverable of the phase but its specification.** That makes it P5-T01, and it
  is the one row that must land before the others are even scoped: until it exists, "feature parity"
  is an opinion.
- **Most of the mechanics exist upstream, again.** `diplomacy.js` (31 exports — tribute, favours,
  goodwill, `PEACE_THRESHOLD`), `wonder.js` (the Antimatter Gate's charge), `victory.js`
  (`checkWinCondition`, endless win/loss, `scoreBreakdown`), and at the galaxy level
  `checkGalaxyRescue` (relief), `surrenderGalaxy`, `checkGalaxyProgress` (milestones),
  `checkDomination`, `checkRivalGate`. **Wiring and presentation for the fourth phase running.**
- **Two things are genuinely new, and both are open questions**: Observer Mode (Q-04, open since
  Phase 0) and audio (Q-06). Neither has any engine support — `grep` finds no `AudioContext`
  anywhere in the vendored tree, so audio is not "reuse upstream's procedural WebAudio" until
  someone checks whether upstream's even came with the engine. Q-06 must be answered on evidence
  before a row is written against it.
- **`scenarios.js` exists and the PRD's Phase 5 scope does not mention it** — escort, raider and
  bounty setups with their own difficulty tables. Either it is parity surface the checklist must
  cover or it is deliberately out of scope; **P5-T01 is where that gets decided**, not here.
- **The reachability rule now applies from the start** (P4-T13): a row is not DONE until a gesture or
  a control produces it, and the runtime import-graph scan will fail on any module nothing opens.

| ID | Task | Status | Deps | Definition of done | Notes |
|---|---|---|---|---|---|
| P5-T01 | Write `docs/planning/PARITY.md` — the checklist Phase 5 is measured against | DONE | — | Every feature of the 2D Odyssey is listed, each marked present / absent / deliberately out of scope, each traceable to an engine export or a UI surface | **The phase's exit criterion is falsifiable for the first time.** Derived from `Object.keys` over the engine's own modules, not from memory, and it **redefines `PRESENT` to mean a player can reach it** — a gesture, a button, and a test that goes red when it is removed. "The logic exists and is proven" was this board's bar for two whole phases and is explicitly not the bar here. **76 present, 29 absent, 12 out of scope, of 117** at the time — and its own count section reported three mutually inconsistent totals until P5-T12 counted the table with a script; it now carries the command that counts it, and stands at **100/4/13** with Phase 5's close-out all but done — §5.2's ten missing orders, the number the file was written to expose, are closed — but the number that matters is §5.2's **18 of 28: ten of the engine's own orders have no gesture, no button and no intent**, a third of the verb list the 2D game gives a player, and two of them (`setRally`, `cancelTrain`) have an intent with **no producer on either end** since Phase 1. `scenarios.js` is ruled OUT with a citation rather than an assertion: ADR-0002 scopes this project to the Odyssey, single-player, and names the three scripted scenarios as a separate product |
| P5-T02 | **Q-06**: audio — reuse upstream's procedural WebAudio, or none | DONE | — | Answered on evidence: does upstream's audio exist in what was vendored, and what would reusing it cost | **ADR-0020: no audio yet, because the bridge discards the events it would speak for.** The board's framing — "reuse upstream's procedural WebAudio, or none" — was checked before being used and the first option does not exist: there is no `AudioContext` anywhere in the vendored tree. **And it found a defect in our own tooling**: `check-bundle-size.mjs` has double-counted every JS chunk since Phase 0, because Vite emits `./assets/x.js` under ADR-0007's relative base while the scrape stripped only a leading `/` — reported **435.8 kB against a true 219.7 kB, exactly double**. Confirmed independently by gzipping `dist` before touching anything, then **fixed** (the ADR had correctly left it recorded, its scope being ADRs not scripts). It never fired wrongly, the error being conservative against 14× headroom — but P5-T02 was told to weigh audio against that number |
| P5-T03 | **Q-04**: Observer Mode, or is a free camera enough | DONE | — | Answered in an ADR with a measurement or a scoped experiment, not a preference | **ADR-0021: no Observer Mode — the snapshot has one viewer.** The camera already exists, so the question was what Observer Mode adds beyond `CameraRig` plus a fog override; the answer is that fog is *not* one flag. Several extractors gate on **visible** versus **explored** differently and deliberately (shots, deaths, alerts, the starmap), so an observer that flipped a single toggle would be inconsistent about what it reveals. Both this and ADR-0020 make the same argument about their own status: **leaving a question open that nobody is scheduled to close is a silent decision**, and now that Phase 5 closes on a checklist, an unruled row makes the exit criterion unmeetable rather than merely unanswered |
| P5-T04 | Diplomacy: stances, tribute, gifts, favours | DONE | P5-T01 | `PEACE_THRESHOLD`, `APPEASE_TIME`, `TRIBUTE_BASE_COST`, `FAVOR_*` all reach the player; a favour's window is visible before it expires | The whole API crosses, and three of its constants are traps rather than readings. `tributeCost` escalates 1.55x a payment, so `TRIBUTE_BASE_COST` is carried as what the FIRST tribute cost and never as the price of the next — the opening 500 credits buy exactly one tribute and not two, which is the escalation made concrete. `FAVOR_WINDOW` is answered the way P4-T07 answered `LANE_PERIOD`: the countdown is walked tick by tick for its whole 90 seconds against the tick `updateDiplomacy` really withdraws the ask on, **including the one tick where the clock reads zero and the ask is still on the state** — `fulfillRequest` refuses in that gap, so the panel stops offering it there rather than a tick later. `stanceLabel`'s bands are FOUND by bisecting `stanceLabel` itself rather than typed in, which is what finally lets P4-T03's unbanded stance bar be banded; the Wary edge lands on `PEACE_THRESHOLD` to eight places. `GRACE_TIME` is reported as the STOCK window and says so in the model: a world's own is `GRACE_TIME × graceMult`, ×1 for the seat under this client's settings but 168–483 s across the roster. `peaceRestsOn` is why the model exists — a stripped world and an untouched one are both "Neutral, at peace" at minute six, and twenty seconds after grace closes only one of them still is; the test runs both on one clock so the war cannot be blamed on the clock. **`hostility` answers 1 for a state with no diplomacy**, which is a skirmish answer about the AI's offense ramp and not an opinion, so it is never forwarded. 25 of 25 mutations caught |
| P5-T05 | The Antimatter Gate and the rival Gate race | DONE | P5-T01 | `updateWonder`'s charge is visible on both sides; `checkRivalGate`'s own state is reported, never re-derived | Charge is a clock, so it is asserted by stepping: one test runs a fed Gate the whole `chargeTime` and holds the model's countdown against elapsed sim time at every step, then watches it come online within one step of the first reading. The harder number is where a *starved* Gate stops — `updateWonder` spends exactly what it banks, so a fixed stockpile carries the charge a fixed distance; the model reports that distance and the test proves it by running 150 s past it. **`chargingWonderOf` answers `null` four different ways** — no Gate, a Gate constructing, a Gate that never took a tick, a Gate already online — and calls a Gate starved for minutes "charging", because it is a range check on `charge` that never looks at the feed. That is P2-T15's `worn` again; `status`/`started` add the distinction and `chargingId` reports the engine's verdict unchanged beside them. The rival is asked for, never looked for: `galaxyStatus().rivalGate` is the same call the bridge makes, so `test/bridge/gate-race.test.ts` holds the panel against `rivalGateIndex` and `alertForWorld`. **And the alert goes out at the worst moment** — `checkRivalGate` nulls its own tracked record on ascension, so the starmap stops drawing the mark exactly when the race is lost, and `rivalAscended` is the only thing on either surface that separates "nobody is racing you" from "somebody finished". That is `longgame.md` task 9 and gate G3. 36 of 36 mutations caught |
| P5-T06 | Milestones, domination and the fireworks | DONE | P5-T01 | `checkGalaxyProgress` and `checkDomination` drive what is shown; `MILESTONE_IDS` round-trip | The engine raises these itself, so the panel reports them: `galaxy.reached` for what has been celebrated, `galaxy.milestones` for the undrained firework queue, `galaxy.pacified` for the conquest count. The trap the row names is a list re-derived from galaxy state, and it is made to bite rather than described — four worlds are marked pacified *without* running `checkDomination`, so the count says the target is met and the engine has raised nothing. `milestoneReached` is `reached.has("domination")`; **`pendingScan` names that window** instead of smoothing it over, because the progress scans run once a second and not once a tick. `MILESTONE_IDS` round-trips twice: the rows are `MILESTONE_IDS.map`, proved by appending to the engine's own array and watching a row appear, and a galaxy with every milestone reached is saved, loaded through `isMilestoneId`'s filter, and shown identically. Neither panel runs a scan — both are mutators, and opening a panel must not raise a firework. **Three mutations survived first**: rows built from this file's own label table (the lists are identical today), and two orderings of the `world:N` family, one of which only breaks past `world:10` — the roster is eleven worlds long. 36 of 36 caught after the fixes |
| P5-T07 | Relief and surrender | DONE | P5-T01 | `checkGalaxyRescue` (with `RELIEF_COOLDOWN`) and `surrenderGalaxy` are reachable and their conditions legible | The cooldown is keyed on `galaxy.time`, not any world's clock, and the panel subtracts exactly as the engine subtracts so the two cannot drift. `footholds` returns the whole list where `checkGalaxyRescue` returns on the first hit — "you still hold Ferros" is the sentence that stops a player surrendering a run they have not lost — and it counts a half-built Command Center, which is the engine's own rule here and *not* the one `checkGalaxyProgress` uses. **`surrenderGalaxy` returns nothing at all** — not even P2-T12's bare `false` — and refuses silently on a seat already over, so the model is three-valued and `applyIntent` asks the question the engine will not answer. `reliefWouldFollow` is true at exactly the moment surrender looks obligatory. **And the row found a defect wider than itself**: the relief ship is minted `"g" + entitySeq` and `numericId` dropped the first character, so `g1` and `u1` packed to the same integer and the one ship a wiped-out player must click could not be selected — pinned here asserting the defect, then fixed in the codec and **inverted** into a regression guard. `longgame.md` task 4 is the only gate in any script in this project set at 5 of 5 |
| P5-T08 | Victory, defeat and the score screen | DONE | P5-T01 | `checkWinCondition`, `checkEndlessWin`/`Loss` and `scoreBreakdown` are shown with the engine's own numbers | `scoreBreakdown` is broken out rather than collapsed into a total, and upstream's own comment says why: "so a HUD can show WHY". **The row's `DEFAULT_MATCH_TIME_LIMIT` is skirmish-only** — `galaxy.js:254` creates every world `endless: true`, so `rules.clock` is null for the whole Odyssey and a countdown here would be inventing one; the skirmish branch is still driven live so a hardcoded null could not pass. `terminalRules` names which of `sim.js`'s three terminal checks a state actually runs, because "would losing every Command Center end this?" has opposite answers in a skirmish and in a galaxy and the difference is the entire premise of P5-T07. Lines are built from `state.owners` — the order the engine iterates and the order its tiebreak depends on — never from `Object.keys`. One thing recorded rather than fixed: **the Ranger is `role: "scout"`**, so an armed unit scores as infrastructure. That is upstream's classification and re-deriving it here would be a second opinion beside the engine's |
| P5-T09 | Full save/load UI | DONE | P5-T01, P4-T09 | A player can save, list and load a galaxy; the round-trip is P4-T09's, not a second implementation | **The row's own note was stale and that was the first finding.** P4-T09's market price-book defect was reported as issue #92, fixed upstream in `50ceb88` and synced here; `galaxy-save.test.ts` now asserts prices *survive*, so a panel warning about it would teach players to distrust a correct save. The round trip is untouched — a source scan proves the panel calls neither `serializeGalaxy` nor `deserializeGalaxy`, and a save goes storage → `bridge.load()` and nowhere else. **The real trap was that DESCRIBING a save is not free**: `deserializeGalaxy` ends in `restoreEntityId`, so the obvious "what is in this file?" would rewind the engine's one global entity-id counter under the running session and mint the next unit on top of a live one — demonstrated in the test, then shown absent. Every number in a row is read off the payload's own top-level fields and asserted against the live galaxy the save came from, so a renamed field is a red test naming the field rather than a row of zeroes: a save reads *Helix Belt · 2 worlds · 1,340 cr · 12m*, because a slot id identifies nothing when every save of one campaign shares it. A save from another version is listed, refused and names both versions. There is no index — the list is storage's own keys, and an unreadable slot is shown rather than hidden, because a row nobody can see is storage nobody can reclaim |
| P5-T10 | Settings and onboarding | DONE | P5-T01 | Settings persist through `loadSettings`/`saveSettings`; a first-time player is told what to press | **The sidebar hint has been wrong since Phase 1 and nothing could have caught it**: `index.html` advertises WASD to pan and `S` is bound to nothing anywhere in the app — `applyContinuousPan` reads `arrowdown` and never `s`. So the card names the arrow keys, and every key it names is DATA, put through the app's own `translateKey` and a real `Game` in a real DOM; the sidebar's list is pinned as an equality so a fix goes red and says to delete the pin. The card does not duplicate that paragraph and does not replace it: the paragraph is a 24-binding reference, the card is the first sixty seconds, six steps in the order the playtest tasks fail in. **A card that cannot be dismissed, or that reappears, is worse than none**, so dismissal is permanent *and* the card retires once the player has a base — which covers the blocked-storage persona without re-teaching a ten-hour campaign on every reload. A step reports `done: null` where the snapshot cannot honestly answer, because an empty checkbox against something nobody measured is the interface calling the player a failure. **Settings gained a row the app did not have**: `TierMonitor.setManual` was a one-way door, so a player who had ever forced a tier could not get detection back without clearing site data — `clearManual` is the Auto option, and it resets the sample window because those frames were measured under a tier the player chose |
| P5-T11 | Phase 5 playtest script | DONE | P5-T04 … P5-T10 | Script exists; written and left UNRUN under ADR-0011 | `docs/playtests/longgame.md`. **The fifth unrun script, and the backlog is now itself the finding** — five scripts asking questions no machine can answer is not four deferrals plus one, it is a project that has never checked its work against a person, and it is filed as a Phase 6 row rather than a footnote. This one is the first that cannot run in the three-script afternoon: two of its scenes (a wipeout, a charged Gate) are unreachable by playing forwards inside a session, so it runs on **four prepared saves** with the states pinned exactly — `MIDGAME` carries 500 credits precisely because the first tribute is 200 and the second 310, so 500 buys one and not two, by ten credits. **Task 1 can be run on any tester exactly once, ever**: the card retires on dismissal and on the player having a base, so any other script run first spends it, and the script says to recruit for that. Two gates are set above this project's usual bar and both say why — **G1 at 5 of 5** (a tester who says "it's over" on a run the engine has not ended fails it outright, because a player closing the tab is a failure no later polish recovers) and G2's dismissal half at 5 of 5. G3 owns P5-T05's own finding and names its fix in advance: the vanished rival mark is a change to the **starmap**, not to the panel |
| P5-T12 | Wire every Phase 5 panel to a control | DONE | P5-T04 … P5-T10 | P4-T13's orphan scan is green; a click in a real DOM produces each panel and the intents it needs | **The fourth phase running that built panels nothing could open.** Phase 2 shipped six of seven orphaned, Phase 4 five, and Phase 5's four parallel agents produced seven more in one afternoon — every one tested to the hilt and reachable by nobody. P4-T13's scan is what turns that from a thing somebody remembers into a red test; what it cannot see is whether a HUMAN can get there, so `test/ui/phase5-wiring.test.ts` drives a real `Game` in a real DOM. Four campaign boards on the starmap, `EconomyBoard`'s idea one screen up: buttons rather than letters, because the positional row already addresses the galaxy drawer and Phase 4 spent every letter this keyboard had left. Only one opens at a time; nine sections is a wall. **Relief is the exception and is not behind a button** — it appears exactly when the engine's rescue clock is doing something, because a player who has just lost everything should not have to go looking. Four intents added (`tribute`, `gift`, `fulfilFavor`, `surrender`), the first three carrying a `planetId` for `colonyPolicy`'s reason. **Two defects fell out of writing the test**: surrender could be pressed twice and say nothing (this HUD keeps a struck-through button clickable on purpose, "the engine's own refusal is what explains it" — and this is the one command where the engine says nothing at all), found by a mutation that survived because no test pressed the button twice; and the graphics tier was a one-way door (see P5-T10). 6 mutations, 6 caught |
| P5-T13 | The ten engine orders with no way in | DONE | P5-T01 | Every one of PARITY §5.2's absent orders has a gesture or a key, and a test fails by name when it is removed | **The number the checklist was written to expose.** 18 of 28: a third of the verb list the 2D game gives a player, with `setRally` and `cancelTrain` carrying an intent and no producer on *either* end since Phase 1. Five of the ten did not cross the façade at all, so the enumeration could see them in the JS and the client could not reach them |
| P5-T14 | The build menu's two lies | DONE | P5-T01 | The unit menu offers every type `queueProduction` would accept, enumerated not listed; affordability is `canAfford`'s answer across all 23 commodities | Both are re-derivations that rotted (ADR-0012 §5), and both were **pinned by a passing test**: `hud.ts` filtered `odysseyOnly` outright on an endless world so six of eighteen units were hidden while `queueProduction` returned true, and affordability knew three commodities of twenty-three so five buildable things read as unaffordable forever. The buttons still fired, because `bind()` only toggles a class — a lie in the HUD rather than a lock, which is the harder kind to notice |
| P5-T15 | The combat cues and the rally line | DONE | P5-T01 | `attackHit`'s `heavy`, `bonus` and `splashRadius` reach the view; `SceneComposer` pushes a rally overlay | Four things the engine already said and nothing heard. The two badges are **categories**, so they are screen-space glyphs at constant size; the splash ring is a **measurement in world units**, so it is ground geometry that deforms over a slope like a selection ring — and it does not grow into its radius, because a ring that expanded would be true only on the frame it expires. They differ on shape, size and stroke weight, never on hue: both draw in the ATTACKER's colour, so hue is already spent. **A plain hit draws nothing at all** — 47 of 203 measured hits carry a flag, and the silence is what makes the cue legible. Fog is live vision at the impact point, deliberately not the *explored* rule deaths use: a death leaves a wreck you walk up to later, a hit is a fight happening now, and it buys back a cost ADR-0017 filed — a player shot from the dark now sees the impact on their own unit while the tracer stays correctly suppressed. 60 of 60 mutations caught; six survived first and every one was the test being wrong, including a spark that degenerated to a regular octagon (radial symmetry alone does not exclude a polygon, and an octagon at 8 px IS the idle badge) |
| P5-T16 | The news nobody hears, and the game nobody sets up | DONE | P5-T01 | `takeColonyNotes` and the three galaxy queues reach a surface; a new-game screen passes `difficulty` and `playerFaction` | `WorldBridge.takeColonyNotes()` is documented, capped and **has no caller** — the only channel saying a colony is under attack while you are elsewhere is collected every tick and dropped. `WorldOptions` has accepted a difficulty and a faction since Phase 1 and `main.ts` has never passed either, so **every session ever played has been medium / frontier** |
| P5-T17 | Close the parity checklist | DONE | P5-T13 … P5-T16 | No row in `PARITY.md` is ABSENT; the count is re-derived, not edited | **104 present, 0 absent, 13 out of 117**, from 76/29/12 when the file was written. §5.2 closed at **28 of 28** — the number the document existed to expose. The count is re-derived by the command §4 now carries, because §4's first draft reported three mutually inconsistent totals. Audio is the one row that moved to OUT rather than PRESENT, under ADR-0020 and with its reopening condition named — and that condition is a client-side one that P5-T15 and P5-T16 have now met, so it reopens on evidence rather than on somebody remembering |

**Carried into Phase 6, with the measurement.** P5-T15 found that **a right-clicked attack draws no tracer at all** — 10 shots, 0 tracers, 10 `dropped` on a Skiff ordered onto a Worker — because `extractShots` reads `unit.autoTarget` and `combat.js:46` takes an ordered attack's target straight off `unit.order`, leaving the only line that writes `autoTarget` unreachable. It is the most common combat order in an RTS and it has produced no feedback since Phase 3. **It was deliberately not patched**: a stopgap would still lose the killing shot and would disguise the case as the ordinary 12.9% one, which is worse than the bug. ADR-0017's premise is corrected in place and the real fix — read `attackHit`, which carries both endpoints and is pushed before the corpse is removed — is P6-T01.

> **Board hygiene, recorded because N-07 asks for it and it had slipped.** Four Phase 4 rows —
> P4-T01, P4-T12, P4-T13 and P4-T14 — read TODO until Phase 6 against work that had shipped in
> Phase 4 and been cited by name in every phase since. P4-T13's orphan scan in particular is the
> rule this board has invoked six times while its own row said it had not been done. That is the
> same failure as the open-questions register carrying eight OPEN rows against settled decisions
> (P6-T08), and `RELEASE.md` names the habit as the one N-requirement with no gate behind it: a
> board that lies about what is finished invites the next reader to redo it.

> **Board hygiene, recorded because N-07 asks for it and it had slipped.** Four Phase 4 rows —
> P4-T01, P4-T12, P4-T13 and P4-T14 — read TODO until Phase 6 against work that shipped in Phase 4
> and has been cited by name in every phase since. P4-T13's orphan scan in particular is the rule
> this board has invoked six times while its own row said it was not done. That is the same failure
> as the open-questions register carrying eight OPEN rows against settled decisions (P6-T08), and
> `RELEASE.md` names the habit as the one N-requirement with no gate behind it: a board that lies
> about what is finished invites the next reader to redo it.

## Phase 6 — Polish and release

Goal: PRD §5, Phase 6 — *it feels finished.* Scope: LOD and effect budgets, accessibility
(colour-blind-safe palettes, keyboard-only play, motion reduction), tutorial, performance passes,
error handling, a release build, docs, versioning and the update check.

*Amended from the repo itself, as Phases 2 through 5 were. Every claim below was checked against the
code before a row was written for it:*

- **N-05 is cited five times and verified nowhere.** `building-panel.ts`, `minimap.ts`, `hud.ts` and
  two rules in `style.css` each say "never colour alone" and each is right — a glyph beside the
  status, squares for buildings against dots for units, a struck-through cost beside the dimming.
  **Not one of them has a test.** They are a convention held by five comments, which is exactly the
  shape of the reachability gap this board has now been caught by five times: a rule everybody
  follows until somebody does not, with nothing to notice. And the *first* clause of N-05 —
  colour-blind-safe owner and faction palettes — has never been checked at all: `grep` finds no
  dichromacy simulation, no palette test, nothing.
- **There is no reduced-motion anything.** N-05 names it, `Settings` does not carry it, and
  `prefers-reduced-motion` appears nowhere in `src/` or `index.html`. Tracers, impact rings, the
  firework queue and the camera's own easing are all unconditional.
- **The HUD cannot be navigated by keyboard.** No `tabindex`, no focus management, no focus styling.
  The positional row (Z/C/V/B/N) reaches the first five buttons of the drawer and nothing else, and
  N-05 says "keyboard-navigable UI" rather than "five hotkeys".
- **A lost WebGL context is a permanently blank canvas.** `webglcontextlost` and `contextrestored`
  appear nowhere. A driver reset, a backgrounded tab on some mobile browsers, or a GPU switch on a
  laptop ends the session with no message — against N-04, which names the last two versions of four
  browsers.
- **A throw inside a frame kills the loop silently.** There is no `try` anywhere in `app/loop.ts` or
  the render path. `main.ts`'s `bootOrExplain` covers the boot and nothing covers the next 10 000
  frames.
- **"The update check" contradicts N-06.** The PRD's Phase 6 scope asks for one and N-06 forbids
  "no network calls beyond loading the app itself". Both are in the same document. One of them has
  to give, and it is a decision with an ADR rather than a thing to quietly implement or quietly
  drop.
- **The version is `0.1.0` in `package.json` and appears nowhere else.** A player who reports a bug
  cannot say which build they were on, and every playtest script asks them to.
- **Five playtest scripts are written and none has met a tester** (P5-T11's finding). That is not
  four deferrals plus one; it is a project that has never checked its work against a person, and it
  is the largest single risk to "it feels finished".
- **The open-questions register is stale.** Q-04 and Q-06 read OPEN and were answered in Phase 5 by
  ADR-0021 and ADR-0020; Q-12 reads OPEN and was answered by ADR-0017, whose premise P5-T15 then
  found to be false. A register that lies about what is decided is worse than no register.

| ID | Task | Status | Depends | Definition of done | Notes |
|---|---|---|---|---|---|
| P6-T01 | A right-clicked attack draws no tracer — read `attackHit` instead of diffing `attackTimer` | DONE | — | An ordered attack draws a tracer; `shots.dropped` is 0 for both ordered and auto-acquired fire; ADR-0017 is superseded by ADR-0023 | **10 fired / 0 tracers / 10 dropped → 10 / 10 / 0.** And the derivation was worse than its own ADR claimed in three further ways, each found by measuring rather than reading: `dropped` was **never actually 0** in a real fight (1–4 per run — a target that died this tick and was never in the previous tick's fog-gated entity table, so `prevPos` had nothing), the test that guarded it **read `dropped` once after the loop** and so could only ever see the final tick, and a shot whose SHOOTER died in the same tick was lost with no counter at all — a per-entity diff has no entity left to read. `livingIds` was an unbounded `Set` added to per entity per tick and never cleared, which also made the prune it guarded a permanent no-op. **AI focus-fire is a third targeting path that never writes `autoTarget`**, so the stopgap would not have fixed that either. One honest regression, stated rather than assumed away: a hit on an armed Helium Bomb detonates it through `detonateIfAttacked` and returns before the push, so that one shot fires and draws nothing — pinned by a **source sweep over `performAttack`'s early returns**, so a miss chance or an out-of-range abort added upstream arrives red rather than silently widening the gap. 23 of 23 mutations caught |
| P6-T02 | N-05, colour: the palettes, and a test that colour is never the only channel | DONE | — | Owner and faction palettes pass a dichromacy check; every place the UI distinguishes two things is asserted to differ on a second channel | **The check found a real failure on shipped content.** The `radioactives` dot (#a6ff8f) measured ΔE00 **3.4** against `credits` under deuteranopia — as close as the textbook red/green control the test carries to prove it can fail at all (2.7). Two of the four top-bar readouts were the same dot, and N-05's colour-alone clause survived only because the commodity's name is printed beside it. Now #fd6eb0, clearing every other dot under all three dichromacies with 18.0 to spare, and fixing a quieter tritanopia near-miss on the way (crystals/radioactives, 11.3 → 77.4). **The first proposed replacement was wrong and checking caught it**: #c9a2ff fixes the reported pair and collides with `crystals` at 5.8, because it had been verified against the pair rather than the set. Method is Machado (2009) at severity 1.0 — chosen over Brettel/Viénot because it is published as a linear-RGB matrix, and every transform removed is one that cannot be silently wrong in the direction that makes a palette test pass vacuously — with CIEDE2000 verified in-test against all 28 reference pairs of Sharma, Wu & Dalal (2005), and three controls each required to be REJECTED under its own deficiency and ACCEPTED under the other two, which kills a transposition a single red/green check would pass. The sweep's exact reach is recorded by mutation rather than asserted: it catches a removed glyph and a severity declared without a style, and NOT a state added to a stylesheet alone inside a group already discharged by a DOM channel — which is right, since CSS for a state nothing emits is dead CSS, but the file says so because an overstated guard is one the next reader trusts too far |
| P6-T03 | N-05, keyboard: the whole HUD reachable without a mouse | DONE | — | Every control the mouse can reach is reachable by keyboard alone, with visible focus, proven by driving a real `Game` in jsdom | `HudFocus` walks every button the HUD draws — which is exactly `Game.actions`, the same ordered list the positional keys index into, so it needs no ids from `hud.ts`. Position of record is `document.activeElement`, not a remembered index, so a mouse click or a browser tab-in continues from where focus visibly is; a rebuild re-anchors on the ACTION ID and lets go rather than stealing focus back if the player moved it first. **Space was a live double-command hazard** the moment anything could hold focus: the browser fires a focused button on Space while `translateKey` reads Space as focus-last-alert — one keystroke, two commands, and the dangerous half presses whatever button you are on. The ring claims Space only while it holds focus, and the test pins both directions. Driven by real `KeyboardEvent`s at the focused element with a window-level spy recording what got past, so "consumed" is a fact about the app rather than a return value. 42 of 42 mutations caught |
| P6-T04 | N-05, motion: a reduced-motion setting that honours the OS | DONE | — | `Settings` carries it, `prefers-reduced-motion` seeds it, and the effects that move obey it | **Every effect keeps its fact and loses its animation** — each cue is a fact (where, whose, which flags, what radius) plus a clock, and reduced motion freezes the clock and touches nothing else. The tracer freezes at FULL strength, because the line is the only channel saying "you are being shot at, from there" and at 20 Hz a fading one reads as a flicker. The death mark freezes at 1/3, where the renderer's two opposed derivations from that number — ring size `0.35+p`, opacity `1−p` — put the most ink on screen the animation ever does. Lifetimes are deliberately unchanged: a clock is not motion, and stretching them would change how much combat a player sees. The setting disagrees with the OS in both directions and re-asks when set back to Auto; `matchMedia` is read once per change, guarded exactly as `localStorage` is. **The row named camera easing and there is none to reduce** — `focusOn` is a hard cut, yaw snaps in eight (ADR-0010), zoom is instantaneous, so the camera already behaves the way this setting would ask; adding easing under a motion-reduction row would be perverse, and `camera.ts` is untouched |
| P6-T05 | Error handling: a lost context, a thrown frame, a corrupt save | DONE | — | `webglcontextlost` is handled and says so; a throw in a frame is caught, reported once, and does not kill the loop; the three are tested | Each fired rather than asserted. **The measured finding that shapes the whole fix: three.js absorbs a lost context so completely that nothing throws** — its own handler calls `preventDefault`, and `WebGLRenderer.render` returns on its first line for as long as the loss lasts, so the loop keeps advancing, `endFrame()` keeps returning plausible stats, the simulation keeps ticking and the picture never changes again. A frame guard cannot see that; a listener is mandatory. The banner names the three real causes, says it is not something the player did, and offers the Canvas2D renderer for a context that never returns. A thrown frame reports once per EPISODE (keyed on phase + error name + message, cleared by one clean frame) and is filed under `failedSteps`, never `droppedSteps` — a bug must never hide inside a slow-machine number. **The corrupt save was the opposite of what this row assumed**: 22 corruptions, every one refused cleanly with the session intact. The live defect was elsewhere — `WorldBridge.load()`'s bare `catch { return false }` discarded eight quotable upstream diagnostics and logged none, and mutated the galaxy before its last throwing statement despite its own header promising otherwise. Both fixed; the pin that recorded the first was inverted, and it needed strengthening because as written it would not have noticed the fix. 55 of 55 mutations caught |
| P6-T06 | The release build, the version, and the update-check ruling | DONE | — | The build stamps a version the player can read and report; an ADR rules on the update check against N-06 | **ADR-0022: no update check.** Ruled on a measured zero — `grep` over `src/`, vendored engine included, finds no `fetch`, `XMLHttpRequest`, `WebSocket`, `EventSource`, `sendBeacon`, service worker or Cache API — so N-06 is true by CONSTRUCTION rather than by policy, and the first network call is the one that costs. The load-bearing argument: on a static Pages deployment with content-hashed assets behind one unhashed `index.html` and no service worker, **the reload IS the update mechanism**, so a check would report something the next reload applies anyway. §5's phrase turned out to be two requirements joined by an `and` — "which build am I on?" is real, unmet and needs no network; "is there a newer build?" needs one and is already answered. What a player loses is stated rather than waved away, including no kill switch for a save-corrupting build, and two reopening triggers are named in advance. The version is injected by `define` from `package.json` plus `git rev-parse` and read in the sidebar, on every failure banner and on the boot-failure screen — stamped BEFORE `boot()` and outside its `try`, so a build that fails to start still says which build failed. Four of the five playtest scripts ask for a commit hash by name. N-06 is now enforced by `test/architecture/no-network.test.ts` rather than claimed by a comment |
| P6-T07 | LOD and effect budgets under the Phase 5 load | DONE | P6-T01, P6-T04 | The perf gate covers a scene carrying the Phase 5 surfaces; LOD thresholds are measured rather than chosen | **The imposter was never billboarded, and that is a shipped defect nobody could see because the gate could not look.** `SceneComposer` pushed `yaw = 0` while both renderers apply only the batch's own Y rotation, and the instance material is a `ShaderMaterial` with no `side` — three.js defaults to `FrontSide`, so a back-facing quad is culled outright. Measured over the rig's eight yaw snaps: **back-facing at five of them, and 0.00 px wide at two more**. `yawIndex` 0 is the BOOT DEFAULT, and past T0's LOD distance a median **57% of on-screen entities are imposters** — so the opening camera drew none of them. It survived five phases because **the gate's camera path visited 2 of the 8 snaps**: `Math.floor(t/6) % 8` against a 10 s run reaches indices 0 and 1 only, so every gated number this project has ever recorded came from two of eight rotations. Fixed with one addition per frame, hoisted out of the loop. Coverage was derived rather than listed and found four more holes: the **build ghost had never rendered in any gated run** (`PerfScene.render` passed `null`, so `pushGhost` never executed — and build mode is the opening minutes of a match), `chevron` drew on 10 frames of 3 000 all by accident, `maxDrawCalls` excluded the 60 warm-up frames so P3's baseline said 44 for a scene that draws 45, and `setPower`'s version gate had neither a test nor a gate. `test/perf/coverage.test.ts` now reads the composer's own push sites against `OVERLAY_STRIDE`, so the sixth hole fails a test instead of waiting for a person. **LOD: `lodDistance` is PICKED** — Phase 1 MVP commit, no working, PRD names no number — and the CPU gate structurally cannot price it: p95 is flat 2.02–2.33 ms from "always imposter" to "never", because the recording renderer shades no fragments and the knob's whole job is removing them. What 340 buys is 33% of T0's triangles. And the imposter is indistinguishable at **no distance the game draws**: it covers 0.73–10.35× the mesh's screen area at the switch, agreeing within a pixel only at 7 151–43 858 units against a 1 100 cull. The cause is the quad using `radius × 2.2` for its HEIGHT on a roster that is mostly wider than tall — a Skiff is 12.4 wide and 2.4 high — so no threshold fixes it; left as a visual decision with an owner rather than retuned to hide it. p95 baselines deliberately **not** re-recorded: six interleaved runs of a clean checkout against the working tree were pairwise indistinguishable, and the clean one blew T0's ceiling twice on a loaded host |
| P6-T08 | Close the open-questions register | DONE | — | Every row is ANSWERED with its ADR, or OPEN with the phase that will answer it | **Eight of nine were already answered and the register did not say so.** Q-04 → ADR-0021, Q-06 → ADR-0020, Q-12 → ADR-0017 (whose premise P5-T15 then found false, recorded in both places). Q-07, Q-08 and Q-10 were answered *in the build* rather than in an ADR — checked against the code, not from memory: buffers are selection-scoped through a pooled map, power is a fog-shaped field behind a version counter, and `src/ui/` holds 22 panel modules against the question's estimate of "six or more". Q-09 → ADR-0013 and ADR-0014. **Only Q-05 is genuinely open**, and Phase 4's upstream round trip gave it its first evidence. A register that reads OPEN against eight settled decisions is worse than no register: it invites the next reader to re-decide something that has already been paid for |
| P6-T09 | Run the five playtest scripts | **BLOCKED ON HUMANS** | P6-T02 … P6-T05 | Five scripts run with real testers; findings filed; the gates in each script scored as written | **The one row on this board that more engineering cannot close.** Deferred under ADR-0011 like every human gate before it, and recorded in `RELEASE.md` §3 as blocked rather than deferred without comment — because **five deferrals is not four deferrals plus one**, it is a project that has never once checked its work against a person. `longgame.md` says so in its own header, being the last script that can. Two of its gates sit above this project's usual bar and one guards a failure no later polish recovers: a player closing the tab on a run the engine has not ended |
| P6-T10 | Phase 6 exit: the release checklist | DONE | P6-T01 … P6-T09 | Every N-requirement has a test or a recorded decision; the build is tagged | `docs/planning/RELEASE.md`, in P5-T01's shape: derived and falsifiable, each row naming the command that goes red rather than asserting the requirement is met, and each carrying what its guard does NOT cover. **Eight of eight enforced, one of them by a process rule** — N-02 is met by mutation testing being the standing bar rather than by a coverage number, which is deliberate: a percentage rewards tests that execute lines without asserting anything, the exact failure mutation testing exists to catch. Four requirements had no citation by name anywhere in the repo and turned out to be enforced under other names — determinism by `test/determinism/`'s recorded fixtures plus the layering test that makes "the renderer may never write" structural, browsers by the Playwright specs, docs by `check:adr`. **Two real gaps are stated rather than smoothed over**: CI runs Chromium only, so Firefox and Safari are covered by the renderer port's conformance suite and by there being no browser-specific code, not by execution; and keyboard-only PLAY is not yet true, which is P6-T11 |
| P6-T11 | Keyboard-only PLAY, not just a keyboard-navigable HUD | DONE | P6-T03 | A player can select units and give every order without touching a mouse, proven by driving a real `Game` | **This row's own premise was wrong, and measuring it made the defect worse.** It said — and an earlier agent's report said — that the opening colony ship starts selected, so `Z` works from a cold start and the problem hides. False: `state.selection` is `[]`, `canDeploy` is false, **the action row is empty**, and the HUD's prompt reads *"Click your colony ship to select it."* A keyboard-only player met a game that told them to use a mouse and offered nothing else. Not a hidden problem — a dead end. A mouse answers two questions the board had no other answer for, **which thing** and **where**, so this is two keys. **Q** is the select-army reservation `intents.ts` has held since the MVP, spent as P5-T13 spent E on scout: bare Q takes the next TYPE, Shift+Q steps through one at a time, and both take the camera with them — the minimap's missing keyboard half. **K** is the last bare letter, spent on the control with no other way in: the camera centre is the cursor, so one press is an armed mode's left click or the context order's right click, and all twelve `PendingMode`s become completable — swept out of the union in the source rather than listed. **Not Enter and not Space, which is the load-bearing negative**: those are the two `hud-focus.ts` owns while the ring holds focus, and giving either a world meaning would rebuild P6-T03's one-keystroke-two-commands hazard on purpose. Two more defects found by writing the tests: the build ghost followed `pointerX/pointerY`, which are 0,0 until a `pointermove`, so a keyboard player was shown the validity, reach and Rig survey of the viewport's **top-left corner**; and the cycle cursor stamped the selection it started from, which cannot tell "my select has not landed" from "a control-group recall put it back" — it stamps the tick now. Proven by playing: a real `Game` founds a base, trains a worker, places a Barracks and moves a unit with **a capture-phase recorder asserting not one mouse or pointer event reached any shell element**, every claim made against the simulation. Still pointer-only and stated rather than waved away: the approach screen's landing mark, and zoom. 34 of 35 mutations caught; the survivor is the synthesised gesture's `type` label, unobservable by construction and recorded in the source as a description rather than a decision |

---

## Phase 7 — Release hardening

**There is no Phase 7 in the PRD.** §5 ends at Phase 6, and Phase 6 is merged. This section is not a
new phase of the product; it is **the backlog Phase 6 wrote down about itself**, collected into rows
so it stops being a set of paragraphs nobody owns. Every row below is a gap already recorded in
`RELEASE.md`, an ADR, or a Notes cell — **nothing here is invented scope**, and the first row is the
one `RELEASE.md` names in those words as "the one a release should close first".

The reason to do this as a phase rather than as a list: five phases running, this project's most
expensive defects were things a guard could not see, and every row here is a guard that is currently
narrower than the claim it stands behind.

| ID | Task | Status | Depends | Definition of done | Notes |
|---|---|---|---|---|---|
| P7-T01 | Firefox and WebKit in CI, not just Chromium | TODO | — | `npm run smoke` runs the e2e suite on all three engines in CI, and any real cross-browser defect is fixed or filed | **`RELEASE.md`'s N-04 row says this is the gap to close first.** `playwright.config` declares one project, `chromium-software`. Firefox and Safari are covered by the renderer port's conformance suite and by there being no browser-specific code — which is an argument, not an execution. The renderer is the surface most likely to disagree: WebGL2 support, `ShaderMaterial` defaults, `EXT_disjoint_timer_query`, canvas sizing and `matchMedia` all differ, and P6-T04 already had to guard `matchMedia` for jsdom |
| P7-T02 | The two defects Phase 6 measured and deliberately did not change | TODO | — | The imposter quad's proportions and the armed-bomb tracer are fixed or ruled on with an ADR | Both were left on purpose and both are recorded with numbers. **The imposter quad** uses `radius × 2.2` for its HEIGHT on a roster mostly wider than tall — a Skiff is 12.4 wide and 2.4 high — so the swap is wrong at every distance and no threshold fixes it (P6-T07 measured 0.73–10.35× the mesh's screen area). It was left because it is a visual decision with an owner. **The armed-bomb tracer**: ADR-0023 §158 records 1 tracer before and 0 after, because `performAttack` returns early when the target is an armed Helium Bomb — the engine sets the bomb off, resets the shooter's cooldown and announces nothing, so there is no `attackHit` to read |
| P7-T03 | The two controls still pointer-only | TODO | — | Zoom and the approach screen's landing mark are reachable from the keyboard | P6-T11 made play keyboard-only and named these two as the remainder rather than waving them away: **the wheel is still the only zoom control in the app**, and the approach screen's landing mark takes screen pixels through `pointAt`, so a jump works but always lands on the engine's default site. N-05 says keyboard-navigable; a control with no keyboard path is the same clause P6-T03 and P6-T11 each closed half of |
| P7-T04 | The four PARTIAL rows, closed or reclassified | TODO | P7-T01 | Each of P1-T23, P2-T20, P3-T03 and P3-T16 is DONE, or PARKED with the reason and the trigger | They have been PARTIAL for four phases. Two are perf gates whose missing half is a browser measurement P7-T01 makes possible; two are playtest-dependent and belong with P6-T09. **A status that has meant the same thing for four phases is not a status** — it is the board hygiene failure that had P4-T13 reading TODO while every phase cited it |
| P7-T05 | Re-derive `RELEASE.md` and close its stale lines | TODO | P7-T01 … P7-T04 | Every row re-checked against the command beside it; no line describes a gap that has since been closed | Its N-05 row already says "keyboard-only PLAY is not yet true — filed as P6-T11", and P6-T11 is done. **That is the file's own warning about itself coming true within one phase**, and it is exactly what `PARITY.md` was wrong about twice. The file says "if a row here disagrees with the command beside it, the command is right" — so run them |
| P7-T06 | The 20-minute soak — Phase 3's own exit criterion, never run | TODO | P7-T02 | A 20-minute AI-vs-player match runs at budget without throwing, and nothing grows without bound | PRD §5 asks Phase 3 for *"a 20-minute AI-vs-player match runs at budget"* and what is gated is a representative frame at the budgeted counts. P3-T16 says why they are different tests: **entity counts drift as units die, which is the exact non-determinism the perf harness was built to avoid** — so a soak cannot assert a fixed draw-call count and has to assert different things. The ones worth asserting are the ones a short run structurally cannot see: that the frame budget holds over 24 000 ticks rather than 600, that **no preallocated table grows without bound** (ADR-0006's zero-per-frame-allocation claim is currently proved on scenes short enough that a slow leak is invisible), and that the match actually progressed — units died and were rebuilt — so the run cannot pass by simulating a stalemate |
| P7-T07 | `detectTier` gives the same machine a different tier in Firefox and Safari | TODO | P7-T01 | The tier guess is browser-independent, or an ADR says why it is not | **Found by P7-T01 and filed rather than fixed, because every available fix silently up-tiers.** `navigator.deviceMemory` is Chromium-only; `detectTier` substitutes `?? 4`, and the consequences are both directions of wrong. **T3 is unreachable in Firefox and Safari** — its gate needs `memory >= 8` and memory is hard-coded 4 — and `TierMonitor` never up-tiers by design, so it never self-corrects: a 16-core discrete-GPU machine gets High in Chrome and Standard in Firefox, permanently, unless the player finds the Settings picker. The dangerous half is the other one: the `memory <= 2` downgrade never fires either, so a 2 GB machine gets **T0 in Chrome and T2 in Firefox** and pays three seconds over budget before the monitor corrects — during the first impression the guess exists to protect. Swept over 4 GPU strings x 5 core counts x 3 memory sizes: **22 of 60 plausible machines disagree**, pinned in `test/view/tiers.test.ts` with the sweep derived in-test so moving the heuristic goes red. The reason it is a row and not a patch: `tiers.ts`'s own header says *"We never silently up-tier"*, and every fix that makes Firefox reach T3 does exactly that for two of four browsers. That is an ADR-0006-shaped decision with an owner. **A second finding from the same mutation run, recorded not changed**: `if (cores >= 8 && memory >= 8) return "T2"` is a provably **equivalent mutant** — the next line returns T2 for everything with `cores >= 4`, so that condition cannot change an answer. It reads as a memory rule and is not one, and the side effect is that **T1 is reachable only at exactly 3 cores** |

## Open questions

Answer before the phase in brackets. Discussion belongs in the PRD §10 entry; the answer belongs
here and in the ADR it produces.

| ID | Question | Needed by | Status |
|---|---|---|---|
| Q-01 | Camera yaw: free orbit or snapped? | Phase 1 (P1-T10) | **ANSWERED** — snapped to 8, ADR-0010. Revisit in Phase 6 with a readability test, not an opinion |
| Q-02 | MVP starts on a fixed world or the seed's draw? | Phase 1 (P1-T01) | **ANSWERED** — fixed, ADR-0010. The world is **Helix Belt**, not `ferros`: Ferros Prime's terrain grid is uniformly open, so an MVP built on it would render a flat plane and demonstrate none of the relief that is the point of the exercise |
| Q-07 | Do per-building commodity buffers cross the bridge every tick, or only for the selection? | Phase 2 (P2-T02) | **ANSWERED in the build — the recommendation, taken.** `Snapshot.buffers` is a `Map` keyed by engine id carrying `input`/`output`/`recipe`/`inputCap`/`outputCap`, and `extractBuffers` fills it **for the selection only**; the whole stockpile crosses once as totals. The map is cleared and refilled from a pool each tick rather than rebuilt, so a player who drags a selection box for ten minutes allocates the same handful of objects (ADR-0006). The rule has since been applied a second time by name: P5-T15's rally lines are selection-scoped on this precedent, because every building is minted with a rally point and drawing all 300 paints an identical stub out of every factory |
| Q-08 | Power/electrification: a second low-res field texture like fog, or a projected overlay? | Phase 2 (P2-T06) | **ANSWERED in the build — the recommendation, taken.** `Snapshot.power` is `{cols, rows, cell, state, version}`: a field of bands at fog resolution behind fog's own version-counter contract, rebuilt only when the set of active sources changes. `extractPower` hashes the sources' ids and states and returns early when the hash is unmoved, which is what keeps a 60 Hz HUD off a per-frame distance-field rebuild |
| Q-09 | 29 building types × 2 owners breaks "one draw call per (mesh, owner)". Merge rare types, or raise the budget? | Phase 2 (P2-T18) | **ANSWERED — ADR-0013: six silhouette families, and the budget was NOT raised.** Nine meshes cover the nineteen buildings the MVP did not draw; ADR-0014 then added two more families and, more importantly, made the ceiling **derived from the roster** rather than chosen, because a hand-picked number has one failure mode and it is the one that kept happening — add a mesh, go red, edit the number. Original finding follows.** ~58 building draws against 15 measured for the whole MVP scene. The answer decides whether PRD §5's "300 buildings" exit criterion is reachable at T0 |
| Q-10 | One pure model per panel, or one growing `hudModel`? | Phase 2 (P2-T09) | **ANSWERED in the build — one model per panel, composed, and the estimate was low.** `src/ui/` holds **22 modules**, not six, and `hud.ts` composes them into `EconomySection`s at three levels (`economyModel`, `galaxyModel`, `galaxySections`). The decision paid for itself in a way the question did not anticipate: because each panel is a pure function testable alone, four agents could write seven of them in parallel in one afternoon. **It also has a documented failure mode, hit four phases running** — a panel that is testable alone is a panel that can be finished and reach nobody, which is what P4-T13's import scan and P5-T12 exist to catch |
| Q-11 | Phase 2's remaining meshes do not fit the frame's draw-call ceiling. Merge them into families, LOD them harder, or raise the ceiling with evidence? | Phase 2 (P2-T05, P2-T17) | **ANSWERED — ADR-0014: families, and the ceiling is DERIVED from the roster rather than chosen.** A hand-picked number has one failure mode and it is the one that keeps happening: add a mesh, go red, edit the number. The test now computes the ceiling from the batching rule over the roster that exists, so adding a mesh changes the derivation in the same commit as the mesh. The buildings cap stays hand-written at 28 — that is the number a new building type should have to argue against. Original finding follows.** Arithmetic: the frame measures 34 batches against a 36 ceiling today. P2-T05's four logistics units add 4 meshes × 2 owners = 8; P2-T17's five extra node types add 5. That is **47 against 36**. Buildings were solved this way once already (ADR-0013, six families) and the same move is available — freighters share a hull, nodes share a rock and differ by the crystal on top — but it is a *decision*, and the third time this budget has been the binding constraint. Recommendation: families again for the nodes (one rock mesh, one crystal mesh, one volatile mesh), and one shared freighter hull with cargo on the existing per-instance channel, which is what P2-T05 asks for anyway. Do NOT raise the ceiling to fit: ADR-0006 calls that the failure it was written about |
| Q-12 | The engine emits no "a shot was fired" event. How does a shot cross the bridge? | Phase 3 (P3-T05, P3-T06) | **ANSWERED TWICE — ADR-0017 chose the diff, ADR-0023 superseded it with the event. The question's own premise was FALSE.** `engine/combat.js:187` pushes an `attackHit` event carrying both endpoints, the owner and three cue flags; P5-T15 reads it for the impact cues and P6-T01 will read it for the tracer. So the derivation was allowed "because there is nothing to ask instead" when there was, and the cost is measured: an ordered attack draws no tracer at all. The ADR carries the correction. Original finding follows.** Measured: thirteen event types exist in the whole simulation and the only combat one is `entityKilled`. There is no tracer, impact or damage-dealt event, so combat feedback cannot be event-driven from the engine. The only per-tick evidence is `unit.attackTimer` resetting to `def.cooldown` and `unit.autoTarget` naming the victim. Options: (a) the bridge diffs those between ticks and synthesises a shot event, (b) the snapshot carries `autoTarget` + a fired flag and the view infers, (c) something else. The sim ticks at 20 Hz and the view renders at 60, so a tracer needs a MOMENT the engine only records as a cooldown reset — and PRD §5 requires no per-frame allocation, which rules out an object per shot. Recommendation: (a), pooled, decided in an ADR before P3-T06 |
| Q-13 | Do wreck and crater flags widen the node table, or ride the existing `comIndex`? | Phase 3 (P3-T08, P3-T09) | **ANSWERED — ADR-0018: their own meshes, decided on a measurement that reverses ADR-0014.** A deposit mesh costs ONE batch (neutral owner, never LOD), against 59 batches of headroom — half what a unit or building mesh costs. ADR-0014 refused a third deposit mesh because metallic-vs-crystalline is a build-menu distinction; salvage-vs-natural is a field one. Origin beats contents on these nodes, and an unknown origin falls back to natural |
| Q-03 | Starmap: true 3D scene or 2.5D diagram? | Phase 4 (P4-T02) | **ANSWERED — ADR-0019: a 2.5D diagram on a plate, because the galaxy has one coordinate.** Not decided on draw calls, which measured 8 either way against a ceiling of 119: `PLANETS` has `x` and no `y`/`z`, the only two engine reads of a world position are both `Math.abs(Δx)`, and a 3D layout must invent 22 numbers nothing can validate. Measured: a galactic shell ranks jump destinations backwards 37.6–45.9% of the time; the best 3D layout ties the plate at ~5.3% while adding marker collisions that overlays cannot resolve, because both renderers draw them to a flat canvas with no depth test |
| Q-04 | Ship Observer Mode, or is a free camera enough? | Phase 5 | **ANSWERED — ADR-0021: no Observer Mode, because the snapshot has one viewer.** The camera already exists, so the question was what Observer Mode adds beyond `CameraRig` plus a fog override; the answer is that fog is not one flag. Several extractors gate on **visible** versus **explored** differently and deliberately — shots, deaths, alerts, the starmap, and now impacts — so an observer that flipped a single toggle would be inconsistent about what it reveals |
| Q-05 | Ever bridge the 3D view back into the 2D repo? | any | OPEN — recommendation: no, and Phase 4 produced the first real evidence either way. **The upstream-first path ran end to end and worked**: three defects found downstream as side effects of testing something else, pinned here as tests asserting the defect, reported as issues #92/#93/#94, fixed upstream in PR #95, and synced back (`93f607ae46fb` → `50ceb88d36f2`) — at which point all three pinned tests went red exactly as designed and were inverted into regression guards. Upstream also improved one of the three fixes rather than applying it, because it could run the real ~41 k-line suite this repo cannot. That is an argument for keeping the boundary rather than dissolving it: the two repos found different things because they are different, and a bridge would make them the same repo with extra steps |
| Q-06 | Audio: reuse upstream procedural WebAudio, or none? | Phase 6 | **ANSWERED — ADR-0020: no audio yet, because the bridge discarded the events it would speak for.** The board's framing was checked before being used and the first option does not exist: there is no `AudioContext` anywhere in the vendored tree, and upstream's `sound.js` was simply never vendored. **The stated reopening condition has since been met** — P5-T15 and P5-T16 now consume `attackHit` and the galaxy note queues — so this reopens on evidence rather than on somebody remembering. PARITY row 105 is OUT rather than ABSENT on exactly this reasoning |
